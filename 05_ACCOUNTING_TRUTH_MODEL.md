# 05 — ACCOUNTING TRUTH MODEL

**النطاق:** Production / قراءة فقط  
**التاريخ:** 14 أغسطس 2026  

**مصدر الحقيقة المقترح والمثبت بالتصميم:** بنود القيود المرحّلة هي المصدر التفصيلي للحركة المحاسبية، مع اعتبار `opening_balance` مكوّناً مستقلاً فقط إذا كان موثقاً ومثبتاً. `financial_transactions` مصدر حالة المستند والمرجع، وليست بديلاً عن بنود القيد. `account_balances_unified` جدول مشتق/تجميعي وليس مصدراً مستقلاً للحقيقة.

المعادلة المؤقتة التي تطابق تصميم `sp_rebuild_balances` هي:

```text
Expected Balance = Opening Balance + Posted Journal Movement
```

لكن التطبيق الرسمي لم يعتمد بعد لأن سياسة العكس والفرع وطبيعة الحساب تختلف بين PHP والإجراء المخزن. النموذج الآمن النهائي يجب أن يضيف سياسة العكس المعتمدة، والتعديلات المعتمدة فقط، وتحويل العملة وفق قاعدة واحدة.

| المصدر | الحقيقة التي يملكها | درجة الثقة |
|---|---|---|
| `journal_lines` | الحركة والقيد التفصيلي | عالية بعد الترحيل |
| `financial_transactions` | حالة السند والمرجع والمبلغ | عالية للحالة، ليست وحيدة للحساب |
| `account_balances_unified` | قيمة مجمعة/مخزنة | مشتقة وتحتاج مصالحة |
| `audit_logs` | أثر الإجراء | رقابة، لا يستبدل القيد |
| `opening_balance` | بداية الحساب إن كانت موثقة | غير مثبتة للفروقات الحالية |

الفرق ليس دليلاً على فساد. الحساب 5/YER بفارق 50,000 ما زال `UNKNOWN` حتى تظهر نسخة تاريخية أو سجل إعادة بناء يثبت المصدر.

## المراجع

[1]: ./ACCOUNT_BALANCE_ROOT_CAUSE_REPORT_20260814.md "تقرير السبب الجذري للأرصدة"
[2]: ./root_cause_findings.json "نتيجة الفحص القراءة فقط"
[3]: ./includes/accounting_functions.php "محرك PHP المحاسبي"
[4]: ./tools/database/alghazali.sql "مخطط وإجراءات قاعدة البيانات"
