# ACCOUNTING RECONCILIATION REPORT

**النظام:** AlGhazali ERP  
**التاريخ:** 14 أغسطس 2026  
**نوع الفحص:** قراءة فقط عبر Web/PHP  
**قاعدة البيانات التي تم فحصها:** `ghazali`  
**محرك قاعدة البيانات:** MariaDB 10.11.14  
**المنفذ الفعلي في Web/PHP:** 3306  
**المضيف:** `a79deb478bd2`  
**القرار:** **NO-GO**

## 1. ملخص تنفيذي

تم تنفيذ فحوصات المصالحة المطلوبة على قاعدة البيانات الفعلية `ghazali` من خلال مسار Web/PHP، مع استخدام استعلامات `SELECT` فقط. لم تُنفذ أوامر `INSERT` أو `UPDATE` أو `DELETE` أو `ALTER` أو `DROP` أو `TRUNCATE` أو Migration، ولم تُعدّل أرصدة أو تُرحّل أو تُنشأ أو تُعكس أي قيود.

أثبت الفحص أن **القيود المرحّلة متوازنة عددياً** في الجولة المصححة، وأن الفواتير المرحّلة لها حركات مالية، وأن روابط الفواتير التشغيلية التي فُحصت لا تشير إلى فواتير مفقودة. في المقابل، توجد اختلافات جوهرية في الأرصدة المخزنة مقارنة بإعادة احتسابها من القيود، وسجل تخصيص يتيم، وحركة مسودة بلا قيود، وست حركات مالية بلا فرع، وفجوات في سجل التدقيق، وبيانات اختبار مرحّلة. لذلك لا يجوز إصدار اعتماد إنتاجي.

> **الحكم المهني:** سلامة التوازن المحلي للقيود لا تكفي لاعتماد الأرصدة والتقارير. وجود فروقات في `account_balances_unified` يجعل القرار النهائي **NO-GO** إلى أن تُفسر الفروقات أو تُعالج لاحقاً في مرحلة إصلاح محكومة وموافق عليها.

## 2. بيئة الاتصال والاختلاف بين CLI وWeb

ملف `.env` الموجود في نسخة CLI يحدد `DB_HOST=127.0.0.1` و`DB_PORT=3307` و`DB_NAME=alghazali_refactor_test`، لكن المنفذ 3307 كان مغلقاً. أما مسار Web/PHP الفعلي فقد اتصل بقاعدة `ghazali` على MariaDB في المنفذ 3306، واستخرج البيانات بنجاح. هذا يثبت أن إعداد Web/PHP ليس مطابقاً لإعداد CLI، أو أن Web/PHP يعمل ببيئة/نسخة ملفات مختلفة أو بالمسار الاحتياطي في `includes/db.php`.

| عنصر الاتصال | Web/PHP الفعلي | CLI/ملف البيئة | الملاحظة |
|---|---|---|---|
| قاعدة البيانات | `ghazali` | `alghazali_refactor_test` | اختلاف جوهري |
| المنفذ | `3306` | `3307` | 3307 غير متاح من CLI |
| المضيف | `a79deb478bd2` | `127.0.0.1` | Web يتصل بخدمة MariaDB داخلية |
| السائق | PDO MySQL | PDO MySQL | متوفر في PHP 8.3 |
| نتيجة الاتصال | ناجح | Connection refused | يجب توحيد الإعدادات خارج الإنتاج قبل أي إصلاح |

لم يتم تغيير أي إعداد لمعالجة هذا الاختلاف، التزاماً بقاعدة القراءة فقط.

## 3. مقارنة مخطط المستودع بقاعدة البيانات الفعلية

تحتوي قاعدة البيانات الفعلية على **154 جدولاً أساسياً، و4 Views، و6 Triggers، و34 Routine**. أما ملف `tools/database/alghazali.sql` في المستودع فيحتوي على نحو **108 تعريفات جداول، و0 Views، و6 Triggers، و0 تعريفات Procedures/Functions** وفق فحص نصي لتعريفات `CREATE`. هذا يعني أن ملف SQL الموجود في المستودع **ليس نسخة كاملة مطابقة للمخطط الفعلي**، ولا ينبغي اعتماده وحده كنسخة استعادة كاملة قبل توحيده والتحقق منه.

| الكيان | الفعلي في `ghazali` | الموجود في SQL المستودع | النتيجة |
|---|---:|---:|---|
| الجداول الأساسية | 154 | 108 تعريفات ظاهرة | **Schema drift**؛ فرق 46 تعريفاً ظاهراً |
| Views | 4 | 0 | Views غير موثقة في ملف المستودع |
| Triggers | 6 | 6 | متطابقة عددياً، وتحتاج مقارنة تعريفية |
| Procedures/Functions | 34 | 0 | إجراءات ودوال غير ممثلة في SQL المستودع |
| الأعمدة | مستخرجة من `information_schema` | موجودة جزئياً في الملف | تحتاج مقارنة آلية تفصيلية |
| Foreign Keys | مستخرجة من القاعدة | علاقات كثيرة ممثلة كفهارس منطقية | خطر سجلات يتيمة عند الحذف أو الترحيل غير المنضبط |

## 4. إجمالي البيانات المالية والتشغيلية

| المؤشر | القيمة الفعلية |
|---|---:|
| إجمالي الفواتير | 57 |
| الفواتير المرحّلة | 36 |
| إجمالي الحركات المالية | 46 |
| الحركات المالية المرحّلة | 42 |
| إجمالي بنود القيود | 90 |
| إجمالي تخصيصات الدفعات | 3 |
| الحسابات الموحدة | 109 |
| أرصدة الحسابات الموحدة | 39 |
| العملات | 3 |
| الفترات المالية | 12 |
| فروع النظام | 1 |
| سجل التدقيق العام | 1,531 |
| السجلات الاختبارية المكتشفة | 49 عينة مطابقة للبحث |

## 5. نتائج سلامة القيود والعلاقات

### 5.1 توازن القيود

تم تجميع المدين والدائن لكل حركة مالية مرحّلة، مع مراعاة الحركة والعملة والفرع والتاريخ. بعد تصحيح استعلام الفحص، كانت النتيجة:

| الفحص | العدد | الحكم |
|---|---:|---|
| `Unbalanced Posted Transactions` | 0 | ناجح من حيث التوازن العددي |
| `Journal Lines Without Parent` | 0 | لا توجد بنود يتيمة بلا حركة مالية |
| فواتير مرحّلة بلا حركة مالية | 0 | ناجح |
| روابط تشغيلية لفواتير غير موجودة | 0 | ناجح في الجداول المفحوصة |

هذه النتيجة تثبت توازن القيود المرحّلة الموجودة، لكنها لا تثبت صحة الأرصدة المجمعة أو اكتمال سجل التدقيق أو سلامة كل التقارير.

### 5.2 حركة مالية بلا قيود

تم العثور على حركة واحدة بلا `journal_lines`:

| رقم الحركة | المعرف | الحالة | النوع | الملاحظة |
|---|---:|---|---|---|
| `B0001` | 459 | `draft` | `receipt` | مسودة بلا قيد؛ لا تُعد خللاً في قيد مرحّل، لكنها تحتاج سياسة واضحة للمسودات |

### 5.3 تخصيص دفعة يتيم

تم العثور على تخصيص واحد يشير إلى فاتورة غير موجودة:

| تخصيص | الحركة المالية | الفاتورة | المبلغ | الخطورة |
|---:|---:|---:|---:|---|
| 42 | 433 | 375 | 250.00 | **HIGH** |

لم يتم حذف التخصيص أو تعديله. يجب تحديد ما إذا كانت الفاتورة حُذفت أو أن التخصيص أُنشئ على معرف خاطئ، ثم إعداد إجراء تصحيحي مستقل مع Rollback.

## 6. نتائج الفواتير والمبالغ

لم يعثر الفحص على اختلافات بين `net_amount` والمعادلة المخزنة في البيانات الحالية، حيث كان الفحص يختبر:

```text
net_amount = total_amount - discount + tax_amount
```

كما لم يعثر على فواتير ذات `amount_received` سالب أو أعلى من صافي الفاتورة، ولم يعثر على تخصيصات تتجاوز صافي الفاتورة في العينة الحالية. هذه نتيجة بيانات حالية، ولا تلغي ملاحظة الكود السابقة بأن مسار الترحيل في `accounting_functions.php` يجب أن يستخدم نفس المعادلة صراحة في جميع حالات الضرائب والخصومات.

| فحص الفواتير | العدد |
|---|---:|
| اختلاف معادلة صافي الفاتورة | 0 |
| حالات `amount_received` غير منطقية | 0 |
| تخصيصات تتجاوز صافي الفاتورة | 0 |
| فواتير مرحّلة بلا حركة | 0 |

## 7. نتائج العملات والفروع والفترات

لم تظهر مشاكل في صلاحية العملات أو أسعار الصرف: لا توجد حركات ذات `currency_id` غير صالح أو `exchange_rate` فارغ/صفر/سالب، ولا بنود قيود بعملة غير موجودة أو قيم مدين/دائن سالبة. كما لم تظهر حركات مرحّلة خارج الفترات المالية المعروفة.

في المقابل، ظهرت **6 حركات مالية بلا `branch_id`**، وهي:

| المعرف | رقم الحركة | النوع |
|---:|---|---|
| 404 | `RCT-26-00035` | قبض معكوس |
| 405 | `PMT-26-00008` | حركة عكسية |
| 406 | `RCT-26-00036` | قبض معكوس |
| 407 | `PMT-26-00009` | حركة عكسية |
| 429 | `RCT-26-00001` | قبض |
| 432 | `RCT-26-00002` | قبض |

الفرع الفارغ لا يكسر توازن القيد، لكنه يضعف التقارير حسب الفرع ويمنع العزو التشغيلي الكامل. لا ينبغي ملؤه تلقائياً بالفرع الوحيد قبل اعتماد سياسة الفروع ومراجعة المستند الأصلي.

## 8. نتائج العكس Reversal

بعد التحقق من حقول `reversal_voucher_id` وروابط الحركة الأصلية والعكسية، لم تظهر مشاكل في فحص وجود الطرفين، ولم تظهر اختلافات في عكس مجموع المدين والدائن للعينة التي تحمل رابط عكس صريحاً.

هذه النتيجة أفضل من الفحص الأولي الذي اعتبر الحقول القديمة `reversal_transaction_id` و`original_voucher_id` إلزامية لكل عكس. الواقع الحالي يستخدم `reversal_voucher_id` في السجلات المفحوصة، ولذلك يجب توحيد سياسة أسماء الحقول مستقبلاً، لا إصلاح البيانات مباشرة.

## 9. اختبار الأرصدة المخزنة مقابل القيود

أُعيد احتساب الرصيد من `journal_lines` للحركات المرحّلة باستخدام `normal_balance` للحساب، ثم قورن مع `account_balances_unified`. ظهرت فروقات في **16 صفاً**. أكبر فرق ظاهر كان:

| الحساب | العملة | الفرع | المخزن | المحسوب | الفرق |
|---:|---:|---|---:|---:|---:|
| 5 | 3 | NULL | 44,500.00 | -5,500.00 | -50,000.00 |
| 164 | 3 | NULL | 35,500.00 | 5,500.00 | -30,000.00 |
| 39 | 3 | NULL | -30,000.00 | 0.00 | 30,000.00 |
| 164 | 1 | NULL | 8,100.00 | -900.00 | -9,000.00 |
| 34 | 1 | NULL | -3,760.00 | 0.00 | 3,760.00 |
| 169 | 3 | NULL | 3,000.00 | 0.00 | -3,000.00 |
| 37 | 1 | NULL | -2,700.00 | 0.00 | 2,700.00 |
| 49 | 1 | NULL | 2,550.00 | 0.00 | -2,550.00 |

**أكبر فرق مطلق ظاهر:** 50,000.00 بعملة الحساب ذي المعرف 5 والعملة 3. لا يجوز اعتبار هذا الفرق خطأً نهائياً قبل التأكد من الرصيد الافتتاحي، والحركات السابقة خارج نطاق الجداول الحالية، وطريقة استخدام `branch_id=NULL`، وما إذا كانت الأرصدة تشمل حركات غير مرحّلة أو تحويلات تاريخية.

لم يتم تنفيذ أي `UPDATE` على `account_balances_unified`، ولم يتم إنشاء قيد تعويضي.

## 10. العملاء والموردون والصناديق والبنوك

تحتوي القاعدة على 3 عملاء و16 مورداً، مع حسابات موحدة وأرصدة متعددة العملات. لكن المصالحة التفصيلية الكاملة لكل عميل ومورد وصندوق وبنك تحتاج أولاً إلى تحديد الحساب الطرفي الرسمي لكل كيان، وسياسة الرصيد الافتتاحي، وقواعد اعتبار الحركة المرحّلة فقط، ثم مقارنة ذلك بتقارير الواجهة. لا يجوز استنتاج رصيد العميل من `invoices` وحدها إذا كانت هناك قيود افتتاحية أو تسويات أو تحويلات مستقلة.

بناءً على فروقات الأرصدة أعلاه، لا يمكن اعتماد أرصدة العملاء والموردين والصناديق والبنوك أو تقرير الربح والخسارة قبل حل نقطة الانحراف بين القيود والأرصدة المخزنة.

## 11. سجل التدقيق Audit Trail

يوجد في القاعدة 1,531 سجلاً في `audit_logs`، لكن فحصاً مباشراً للحركات المالية وجد **39 حركة مالية بلا سجل مطابق في `audit_logs`** عند البحث على `table_name='financial_transactions'` و`record_id`. كما أن جدولَي `financial_transaction_audit` و`financial_transaction_logs` يحتويان على صفر سجل وفق العينة الحالية.

هذه النتيجة لا تعني بالضرورة أن كل حركة بلا أي أثر؛ فقد تكون بعض العمليات مسجلة تحت جدول `invoices` أو `module_audit_log`، لكنها تعني أن ربط الحركة المالية نفسها بالتدقيق غير مكتمل. هذا تصنيف **HIGH** لأن الترحيل والعكس والقبض والصرف يجب أن يكون لها أثر قابل للتتبع.

## 12. بيانات الاختبار

تم العثور على **49 سجلاً** مطابقاً لمؤشرات الاختبار أو الوصف الاختباري في `invoices` و`financial_transactions`. العينة تتضمن حركات مرحّلة مثل `JRN-26-00002` و`INV-26-00005` إلى `INV-26-00017` و`PUR-26-00015` وما بعدها، ما يعني أن بيانات الاختبار قد تؤثر على الأرصدة والتقارير.

لم يتم حذف أي سجل. قبل أي تنظيف يجب إنشاء تقرير كامل بكل سجل، وحالته، وقيده، وتأثيره على الرصيد والتقارير وسجل التدقيق، ثم اعتماد سياسة: إبقاء، أو عكس محاسبي، أو نقل إلى بيئة اختبار، أو استبعاد رسمي في التقارير.

## 13. جدول الاختلافات والتصنيف

| المشكلة | المصدر | القيمة المتوقعة | القيمة الفعلية | الفرق | الخطورة | الإجراء |
|---|---|---:|---:|---:|---|---|
| فروقات الأرصدة المجمعة | `journal_lines` مقابل `account_balances_unified` | فرق = 0 | 16 صفاً مختلفاً | أكبر فرق مطلق 50,000.00 | **CRITICAL** | تحليل افتتاحيات وفروع وسعر صرف؛ لا تعديل مباشر |
| تخصيص يتيم | `payment_allocations.id=42` | فاتورة موجودة | `invoice_id=375` مفقود | 250.00 | **HIGH** | تحديد المستند الصحيح وإجراء تصحيحي معتمد |
| فجوات تدقيق للحركات المالية | `financial_transactions` و`audit_logs` | سجل لكل عملية حساسة | 39 حركة بلا تطابق مباشر | غير مالي مباشرة | **HIGH** | توحيد مصدر Audit Trail وفحص العمليات التاريخية |
| حركات مرحّلة بلا فرع | `financial_transactions` | فرع صالح | 6 حركات `branch_id=NULL` | غير محدد | **HIGH** | مراجعة مستند المصدر وسياسة الفرع |
| بيانات اختبار مرحّلة | `invoices` و`financial_transactions` | بيانات إنتاج معتمدة فقط | 49 عينة اختبارية | قد تؤثر على التقارير | **HIGH** | لا حذف؛ تصنيف واعتماد معالجة لاحقة |
| حركة مسودة بلا قيد | `financial_transactions.id=459` | لا قيد للمسودة أو حالة واضحة | `B0001` بلا قيود | 0 حالياً | **MEDIUM** | إبقاء كمسودة أو إلغاؤها عبر مسار رسمي |
| مخطط المستودع غير مكتمل | SQL المستودع مقابل `information_schema` | تطابق كامل | 154 جدولاً فعلياً مقابل 108 تعريفات ظاهرة، و34 Routine مقابل 0 | فرق مخطط | **HIGH** | استخراج Schema رسمي كامل قبل أي Migration |
| فواتير/قيود غير متوازنة | `financial_transactions` و`journal_lines` | 0 | 0 | 0 | **INFO/GO** | استمرار المراقبة فقط |
| مشاكل العملة | الحركات وبنود القيود والعملات | 0 | 0 | 0 | **INFO/GO** | لا إجراء تصحيحي |
| مشاكل الفترات | الحركات المرحّلة والفترات | 0 | 0 | 0 | **INFO/GO** | لا إجراء تصحيحي |
| مشاكل العكس | الحركات الأصلية والعكسية | 0 في الفحوص الحالية | 0 | 0 | **INFO/GO** | توحيد سياسة الحقول لاحقاً |

## 14. إجابات المؤشرات المطلوبة

| المؤشر المطلوب | النتيجة |
|---|---:|
| إجمالي الفواتير | 57 |
| إجمالي الحركات المالية | 46 |
| إجمالي القيود | 90 بند قيد |
| عدد القيود غير المتوازنة | 0 للحركات المرحّلة |
| عدد الفواتير بدون قيود مالية | 0 للمرحّلة |
| عدد القيود بدون مصدر/حركة أصل | 0 بنود يتيمة |
| عدد السجلات اليتيمة | 2 رئيسية: حركة مسودة بلا قيد + تخصيص بلا فاتورة |
| عدد اختلافات الأرصدة | 16 صفاً |
| عدد مشاكل العملات | 0 |
| عدد مشاكل الفترات | 0 |
| عدد مشاكل العكس | 0 وفق الروابط الصريحة الحالية |
| عدد مشاكل الدفع والتخصيص | 1 تخصيص يتيم |
| عدد مشاكل الصلاحيات | لم يُنفذ اختبار تصعيد صلاحيات تشغيلي؛ لا يُعتمد على هذا التقرير لإغلاقها |
| عدد مشاكل Audit Trail | 39 فجوة مباشرة للحركات المالية |
| عدد السجلات الاختبارية | 49 عينة |
| إجمالي الفروقات المالية | غير قابل للتجميع بأمان قبل توحيد الرصيد الافتتاحي والفرع والعملة |
| أكبر فرق | 50,000.00 في العينة الظاهرة |
| أكثر حساب متأثر | الحساب 5 |
| أكثر فرع متأثر | `branch_id=NULL`؛ يوجد فرع فعلي واحد |
| الحكم النهائي | **NO-GO** |

## 15. التوصية وخطة المرحلة التالية

يجب التوقف عند هذا التقرير وعدم بدء Migration أو Cleanup أو تعديل كود أو تعديل قاعدة البيانات. المرحلة التالية، بعد مراجعة واعتماد النتائج، يجب أن تكون **PHASE 3 — CONTROLLED ACCOUNTING FIXES**، وتبدأ بنسخة احتياطية قابلة للاستعادة واختبار استعادة فعلي في بيئة منفصلة.

الأولوية الأولى هي تحليل فروقات `account_balances_unified` من مصدرها: الرصيد الافتتاحي، الحركات المرحّلة، الفرع، العملة، وسعر الصرف. الأولوية الثانية هي تتبع التخصيص 42 والحركة 433 والفاتورة 375 قبل أي حذف أو تعديل. الأولوية الثالثة هي توثيق 39 فجوة Audit Trail و49 سجلاً اختبارياً، ثم بناء خطة معالجة منفصلة لكل مجموعة. الأولوية الرابعة هي استخراج مخطط فعلي كامل من قاعدة البيانات ومقارنته آلياً مع المستودع قبل أي Migration.

## 16. الملفات الداعمة

تم حفظ نتيجة المصالحة الخام وملخص الاستثناءات في الملفات التالية، وهي قراءات ناتجة عن الفحص وليست أوامر تعديل:

- `reconciliation_summary_run2.json`
- `reconciliation_findings.json`
- `tools/database/alghazali.sql`
- `includes/accounting_functions.php`
- `FINANCE_PRODUCTION_READINESS_REPORT_V2_20260807.md`

## المراجع

[1]: ./pasted_content_2.txt "متطلبات المرحلة الثانية للتدقيق والمصالحة"

[2]: ./tools/database/alghazali.sql "نسخة SQL الموجودة في المستودع"

[3]: ./includes/accounting_functions.php "مسار الترحيل وتحديث الأرصدة"

[4]: ./FINANCE_PRODUCTION_READINESS_REPORT_V2_20260807.md "تقرير إصلاحات P0 والاختبارات المعزولة"
