# دليل التثبيت والنشر العربي

## نظرة عامة

هذا المشروع هو منصة حجوزات باصات عربية تعمل بـ **PHP 8+** و**MySQL/InnoDB**، وتضم واجهة عميل وواجهة مدير وواجهة وكيل، وواجهة REST API تحت المسار `api/v1`. صُمم المشروع ليُرفع مباشرة على استضافة PHP مشتركة مثل InfinityFree، من دون Composer أو Node.js أو عملية خلفية دائمة.

> لا يضع المشروع بيانات اتصال قاعدة البيانات داخل مستودع الشيفرة. يجب إنشاء `config/config.php` محليًا على الخادم انطلاقًا من `config/config.php.example`، ثم منع الوصول المباشر إلى هذا المجلد بواسطة ملف `.htaccess` المتضمن.

| المتطلب | القيمة المقترحة |
|---|---|
| إصدار PHP | 8.1 أو أحدث |
| قاعدة البيانات | MySQL 5.7.8+ أو MariaDB داعمة لـ JSON وInnoDB |
| الترميز | `utf8mb4` و`utf8mb4_unicode_ci` |
| الخادم | Apache مع `mod_rewrite`، أو نقطة دخول `api/v1/index.php` المباشرة |
| المنطقة الزمنية الافتراضية | `Asia/Aden`، قابلة للتعديل في الإعدادات |

## التثبيت على InfinityFree

أنشئ موقعًا وقاعدة بيانات MySQL من لوحة الاستضافة، ثم انسخ بيانات المضيف واسم قاعدة البيانات والمستخدم وكلمة المرور. ارفع **محتويات** مجلد `infinityfree/` إلى مجلد `htdocs/`؛ لا ترفع المجلد الأب نفسه حتى تبقى الروابط النسبية سليمة.

انسخ الملف `config/config.php.example` إلى `config/config.php`، ثم عدّل القيم التالية. استخدم رابط HTTPS الفعلي للموقع في `app_url`.

```php
'app_url' => 'https://example.epizy.com',
'database' => [
    'host' => 'sqlXXX.infinityfree.com',
    'port' => 3306,
    'name' => 'epiz_XXXXXXXX_bus_booking',
    'username' => 'epiz_XXXXXXXX',
    'password' => 'ضع_كلمة_مرور_قاعدة_البيانات_هنا',
    'charset' => 'utf8mb4',
],
```

بعد إنشاء قاعدة البيانات، افتح phpMyAdmin، واختر القاعدة المنشأة، ثم استورد `database/schema.sql`. ينشئ الملف الجداول والمفاتيح الأجنبية والفهارس. بعد ذلك يمكن استيراد `database/seed.sql` **في بيئة تجريبية فقط** لإنشاء بيانات العرض وحسابات الاختبار. لا تستورد بيانات البداية في بيئة إنتاج تتضمن بيانات تشغيلية، أو غيّر كلمات مرور الحسابات التجريبية وعطّلها فورًا.

| الرابط | الغرض |
|---|---|
| `/customer.php` | واجهة العميل والبحث والحجز والتذاكر |
| `/admin.php` | لوحة مدير الشركة والإشراف على الحجوزات والتقارير |
| `/agent.php` | لوحة الوكيل والمحفظة وكشف الحساب والعمولات |
| `/api/v1/index.php?route=health` | فحص جاهزية API |

## متغيرات السلامة قبل الإطلاق

غيّر كل كلمات مرور الحسابات الأولية، واحذف أو عطّل حسابات العرض غير المستخدمة. لا تحفظ ملف `config/config.php` ضمن نسخة عامة، ولا تمنح مستخدم قاعدة البيانات صلاحيات أعلى من المطلوبة للتطبيق. تأكد كذلك من تشغيل الموقع عبر HTTPS؛ الجلسات تُنشأ بخصائص `HttpOnly` و`SameSite=Lax`، وتصبح `Secure` تلقائيًا مع HTTPS.

## انتهاء صلاحية الحجز على InfinityFree المجاني

اختير وضع الاستضافة المجانية بناءً على القرار المعتمد للمشروع. لا يستخدم هذا الوضع Cron Job أو طلبات خلفية مخالفة للشروط. بدلًا من ذلك تستدعي نقاط الدخول ذات الصلة، مثل البحث عن رحلة، عرض المقاعد، لوحة الحجوزات، ولوحة التقارير، عملية `expirePendingBookings()` قبل إعادة البيانات. تُحوّل طلبات الانتظار المنتهية إلى `expired`، وتسجّل سبب الإلغاء في سجل التدقيق، وتصبح المقاعد متاحة عند أول تفاعل لاحق في النظام.

> هذا يعني أن المقعد لا يتحرر في الدقيقة نفسها عند غياب أي تفاعل بالموقع. لشرط تحرير زمني مضمون يجب نقل التطبيق إلى استضافة تدعم Cron Job وتشغيل مهمة منفصلة كل دقيقة أو خمس دقائق. أكد مجتمع InfinityFree أن Cron Jobs غير متاحة ضمن الاستضافة المجانية. [1] [2]

## حسابات العرض

هذه الحسابات مخصّصة للتثبيت التجريبي بعد استيراد `seed.sql`:

| الدور | البريد الإلكتروني | كلمة المرور التجريبية |
|---|---|---|
| المدير الرئيسي | `admin@example.com` | `Admin@123` |
| مدير الشركة | `company@example.com` | `Company@123` |
| الوكيل | `agent@example.com` | `Agent@123` |
| العميل | `customer@example.com` | `Customer@123` |

## استكشاف الأعطال

إذا أعطى رابط `health` خطأ اتصال، راجع قيم قاعدة البيانات في `config/config.php` وتأكد أن IP الخادم مسموح في لوحة الاستضافة. إذا لم تعمل المسارات ذات الصيغة النظيفة، استعمل دائمًا العنوان المركزي `api/v1/index.php?route=...`، وهو العنوان الذي تستخدمه الواجهة افتراضيًا ويعمل حتى عند عدم تفعيل إعادة الكتابة. لا تعرض رسائل PDO التفصيلية للمستخدم؛ يتم تسجيلها في سجل خادم PHP فقط في وضع الإنتاج.

## المراجع

[1]: https://forum.infinityfree.com/t/cron-job-feature-is-now-disabled/80495 "InfinityFree: Cron job feature is disabled"
[2]: https://forum.infinityfree.com/t/cron-jobs/108641 "InfinityFree community: Cron Jobs are unavailable"
