# تقرير ضمان الجودة والأمان لمنصة رِحلة

**الإصدار المفحوص:** `50b4744`  
**تاريخ التقرير:** 2026-09-15  
**النطاق:** فحص الكود، الأمان، OTP، ملفات النشر، والاتصال العام بالاستضافة.

## ملخص الحالة

النسخة البرمجية قابلة للفحص محليًا، وجميع ملفات PHP التي تم فحصها سليمة نحويًا، كما أن ملف JavaScript الرئيسي سليم. الملفات البرمجية الأساسية والتحديثات الأمنية موجودة على GitHub والاستضافة. لا يمكن إعلان جاهزية إنتاجية كاملة لأن صفحات PHP وواجهة API في InfinityFree تتعرض لمهلات اتصال، بينما `robots.txt` يستجيب.

| المؤشر | النتيجة |
|---|---|
| أخطاء PHP syntax | 0 |
| أخطاء JavaScript syntax | 0 |
| تغييرات محلية غير محفوظة | 0 بعد الإصدار |
| ملفات config أو env متتبعة في Git | 0 |
| اختبار OTP المحلي المستقل | ناجح |
| مطابقة ملف `.htaccess` محليًا وبعيدًا | ناجحة |
| `robots.txt` الحي | HTTP 200 |
| الصفحات الديناميكية الحية | مهلة متقطعة/غير متاحة أثناء الفحص |

## سجل المشاكل والإصلاحات

| ID | المشكلة | المجال | الخطورة | السبب | الإصلاح | الحالة |
|---|---|---|---|---|---|---|
| SEC-001 | إمكانية تجاوز حد الإرسال عبر إعادة إرسال OTP داخل الجلسة | OTP | High | كان المعدل يحسب عدد الجلسات لا مجموع `send_count` | تعديل الاستعلام ليحسب مجموع الإرسالات خلال الساعة واليوم | Fixed |
| OTP-001 | رفض بعض أرقام الهاتف المحلية والعربية | OTP | Medium | التطبيع لم يقبل الصفر المحلي والأرقام العربية في الحالة المطلوبة | توسيع `normalizePhone` مع الحفاظ على التحقق الدولي | Fixed |
| SEC-002 | ضعف ترويسات الحماية والوصول المباشر للمجلدات الداخلية | Server | High | إعدادات Apache لم تكن تمنع جميع المسارات الداخلية | إضافة ترويسات أمنية ومنع الوصول إلى `config` و`includes` و`classes` و`database` | Fixed |
| PERF-001 | ظهور CSS وJavaScript قديم بعد النشر | Frontend | Medium | أرقام cache-busting قديمة في القوالب | تحديث أرقام أصول CSS وJS في القوالب العامة والإدارية | Fixed |
| HOST-001 | مهلة صفحات PHP وAPI في الاستضافة | Production | Critical | الخادم أو اتصال PHP/MySQL في InfinityFree لا يعيد استجابة مستقرة | يحتاج سجل أخطاء الاستضافة وفحص إعدادات الاتصال أو نقل الإنتاج | Open |
| QA-001 | لا يوجد اختبار تكامل آلي كامل بقاعدة بيانات اختبار | QA | High | لا توجد بيئة DB اختبار مستقلة متاحة في الجلسة | يحتاج قاعدة اختبار معزولة أو تشغيل محلي مكتمل | Open |

## القيود الحالية

لم يتم إرسال OTP حقيقي إلى بريد أو هاتف؛ تم تشغيل اختبار مستقل للتوليد والتطبيع فقط، لأن الإرسال يحتاج مزودًا مضبوطًا وبيانات اعتماد حقيقية. ولم يتم تنفيذ سيناريو حجز ودفع كامل على الإنتاج لأن صفحات PHP الحية تعرض مهلات اتصال، ولأن تنفيذ عمليات مالية حقيقية يحتاج بيانات اختبار وموافقة تشغيلية.

## قرار الإصدار

الإصدار **آمن للنشر البرمجي المحدود** من ناحية الفحص النحوي وحماية الملفات وOTP، لكنه **ليس جاهزًا لإعلان الجاهزية التجارية الكاملة** قبل حل `HOST-001` وتشغيل اختبارات التكامل على قاعدة بيانات معزولة واختبار مسار الحجز والدفع من البداية إلى النهاية.
