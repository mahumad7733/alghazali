# Refactoring Issues

## ISSUE-001: MySQL غير متاح أثناء التحقق

- الوصف: اختبار `tools/test_signature_compat.php` توقف بسبب رفض الاتصال بـ `127.0.0.1:3306`.
- السبب: خدمة MySQL المحلية غير متاحة وقت التحقق.
- الأثر: تعذر إثبات عمليات Stored Procedures والتكامل المالي على قاعدة اختبار.
- الحل: تشغيل MySQL، أخذ نسخة اختبار، ثم تشغيل اختبارات التكامل قبل اعتماد المراحل النهائية.
- الحالة: مفتوح.

## ISSUE-003: تعارض Collation داخل Stored Procedure

- الوصف: `tools/run_integration_test.php` يفشل عند `sp_create_invoice` بخطأ `Illegal mix of collations` بين `utf8mb4_unicode_ci` و`utf8mb4_general_ci`.
- التحقق: تم تشغيل MySQL على المنفذ 3307 باستخدام قاعدة `alghazali` من `.env`، وإعادة المحاولة بعد ضبط Collation لجلسة الاختبار، واستمر الخطأ.
- الأثر: لا يمكن اعتماد التكامل المالي أو مقارنة الأرصدة قبل معالجة Collation في بيئة اختبار.
- الحل الآمن: تدقيق Collation للإجراءات والجداول ثم إعداد Migration/خطة تصحيح منفصلة مع Backup وRollback؛ ممنوع الإصلاح المباشر على الإنتاج أثناء إعادة الهيكلة.
- الحالة: مفتوح، ويمنع اعتماد الاختبارات المالية النهائية.

## ISSUE-002: نقل المنطق المالي تدريجيًا

- الوصف: الخدمات الجديدة تعمل كطبقة تفويض إلى `LegacyFinanceService` للحفاظ على التوافق.
- السبب: منع نقل منطق مالي حساس دفعة واحدة قبل اكتمال اختبارات التكامل.
- الأثر: المرحلة المعمارية مكتملة وظيفيًا، لكن النقل الداخلي الكامل للمنطق لم يُعتمد بعد.
- الحل: نقل كل مجموعة عمليات إلى خدمتها مع اختبار مقارنة قبل/بعد وCommit مستقل.
- الحالة: مفتوح.
