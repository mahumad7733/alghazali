# حزمة مشروع رِحلة

هذه النسخة تحتوي على ملفات تطبيق حجز الباصات الحالية المبنية بـ **PHP/MariaDB/JavaScript**، إضافة إلى تصدير قاعدة البيانات المحلية الفعلي في `database/rihla_local_dump.sql` ونسخة `rihla.sql` في جذر المشروع.

## إعداد الاتصال

لأسباب أمنية، لا تتضمن الحزمة ملف الاتصال المحلي الذي يحتوي على كلمة المرور. انسخ `config/config.php.example` إلى `config/config.php` ثم املأ قيم `app_url` وبيانات قاعدة MariaDB المناسبة لبيئتك.

## استيراد قاعدة البيانات

أنشئ قاعدة بيانات MariaDB فارغة بترميز `utf8mb4`، ثم استورد `database/rihla_local_dump.sql` عبر phpMyAdmin أو الأمر التالي:

```bash
mysql -u USERNAME -p DATABASE_NAME < database/rihla_local_dump.sql
```

بعد ذلك راجع `config/config.php`، وتأكد من أن مسار رفع الملفات `uploads/` قابل للكتابة في بيئة الاستضافة أو الخادم المحلي.

## التشغيل المحلي

شغّل المشروع من مجلد التطبيق عبر PHP 8.3 أو أحدث، مثل:

```bash
php -S 0.0.0.0:8082
```

ثم افتح `http://127.0.0.1:8082/customer.php` للواجهة العامة أو `http://127.0.0.1:8082/admin/admin.php` للوحة الإدارة.

## ما تم تحسينه في هذه النسخة

تتضمن النسخة قسم «تهيئة النظام» كبطاقات منظمة، والبحث الديناميكي داخل الشريط الجانبي، ونافذة الخريطة المنبثقة للمحطات مع بحث جغرافي واختيار الإحداثيات، وحقول التحويل البنكي ورقم العملية وإشعار التحويل الاختياري، وإظهار حالة الدفع ونوعه في التذاكر، وزر حفظ التذكرة كصورة، إضافة إلى دعم الوضع الليلي في الواجهة العامة ولوحات الإدارة والوكيل وصفحة الدخول.

> لم تُرفع هذه النسخة إلى InfinityFree أو GitHub ضمن هذه العملية.

## إعداد OTP والتسجيل الآمن

أُضيفت خدمة OTP قابلة للتفعيل من لوحة الإدارة عبر `admin/otp_settings.php`. وهي معطلة افتراضيًا حتى لا يتعطل تسجيل الدخول في نسخة لم تُضبط لها قنوات إرسال حقيقية. عند التفعيل يمكن اختيار البريد الإلكتروني أو SMS أو WhatsApp، مع مدة صلاحية، عدد محاولات، ومحددات لإعادة الإرسال. يُحفظ رمز التحقق على شكل تجزئة فقط، وتُقنّع الوجهات في سجل الإدارة، وتُشفّر مفاتيح المزودين داخل قاعدة البيانات.

قبل تفعيل القناة، أدخل إعدادات مزود حقيقي من صفحة إعدادات OTP واستخدم زر الاختبار. البريد يدعم SMTP أو `mail()` عند توفره، أما SMS وWhatsApp فيحتاجان عنوان API ورمز وصول صحيحين من مزود الخدمة. لا تضع مفاتيح المزودين داخل ملفات المشروع أو المستودع؛ استخدم الحقول السرية في لوحة الإدارة، واضبط `otp_encryption_key` في `config/config.php` من قيمة عشوائية طويلة عند النشر.

مسارات API الأساسية هي `auth/otp/channels` لجلب القنوات المتاحة، و`auth/otp/registration/request` و`auth/otp/login/request` لطلب الرمز، و`auth/otp/verify` للتحقق، و`auth/otp/resend` لإعادة الإرسال. جميع العمليات التي تغيّر حالة التحقق محمية بجلسة CSRF، ولا تُنشأ حسابات العملاء قبل نجاح التحقق.
