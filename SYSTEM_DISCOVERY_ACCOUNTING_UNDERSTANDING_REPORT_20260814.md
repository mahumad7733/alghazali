# تقرير اكتشاف النظام والفهم المحاسبي

**النظام:** AlGhazali ERP

**تاريخ التقرير:** 14 أغسطس 2026

**إعداد:** Manus AI

**نطاق العمل:** تدقيق ساكن وقراءة فقط للكود والمخطط ونسخة SQL الموجودة في المستودع، مع اختبار اتصال محلي غير كتابي. لم تُنفَّ أي أوامر `INSERT` أو `UPDATE` أو `DELETE` أو `ALTER` أو ترحيل على قاعدة بيانات الإنتاج.

## 1. القرار التنفيذي

> **القرار الحالي: NO-GO للإنتاج غير المشروط.**
>
> طبقة الكود المحاسبي الأساسية قطعت شوطاً جيداً، وإصلاحات P0 السابقة اجتازت الاختبارات المعزولة، لكن لا يمكن اعتماد النظام إنتاجياً قبل إتمام مصالحة قراءة فقط على قاعدة البيانات الفعلية، واختبار HTTP تشغيلي مضبوط، ومعالجة ملاحظات اتساق محاسبي محددة.

السبب ليس وجود دليل على تلف شامل في البيانات، بل لأن الاتصال بقاعدة البيانات الفعلية لم ينجح من بيئة CLI أثناء هذه الجولة. لذلك فإن أي حكم على الأرصدة الحية أو اكتمال العلاقات أو سلامة جميع السجلات سيظل **غير مثبت ميدانياً** حتى يُسمح بقراءة قاعدة البيانات الصحيحة أو تُوفَّر نسخة Staging مطابقة.

| بوابة الاعتماد | الحالة | الدليل |
|---|---:|---|
| سلامة كود الإصلاحات الحرجة السابقة | GO جزئياً | تقرير الجاهزية المالية السابق يذكر نجاح 27/27 lint و29/29 اختبار تكامل معزول [1] |
| سلامة بنية الدورة المحاسبية | GO مع تحفظات | الجداول والقيود والقوادح ومسار الترحيل موجودة في المخطط والكود [2] [3] |
| المصالحة على قاعدة البيانات الحية | NO-GO | اختبار PHP CLI انتهى بـ `SQLSTATE[HY000] [2002] Connection refused` |
| اختبار الإنتاج HTTP الشامل | NO-GO | لم يُنفذ في هذه الجولة لتجنب تغيير البيانات أو تنفيذ عمليات مالية |
| اعتماد الإنتاج | NO-GO | يتطلب نسخة احتياطية قابلة للاستعادة ومصالحة موثقة وموافقة تشغيلية |

## 2. نطاق النظام المكتشف

النظام ليس مجرد شاشات حجوزات؛ بل يتكون من ثلاث طبقات مترابطة. الطبقة التشغيلية تشمل الحجوزات، العمرة، الحج، الجوازات، تأشيرات العمل، الزيارات العائلية، البريد، والموردين. الطبقة المستندية تحفظ الفواتير في `invoices`، وتربط المستند التشغيلي عبر `source_type` و`source_id`، كما تحمل بعض الوحدات معرفي فاتورة البيع والشراء داخل السجل التشغيلي. الطبقة المحاسبية تحفظ رأس الحركة في `financial_transactions` وبنود القيد في `journal_lines`، ثم تُحدِّث أرصدة الحسابات في `account_balances_unified`.

يُدار سير العمل في جداول منفصلة مثل `workflows` و`workflow_steps` و`workflow_transitions` و`workflow_field_values`. وهذا فصل صحيح من حيث التصميم؛ إذ ينبغي ألا يؤدي تغيير حالة تشغيلية إلى قيد مالي إلا عند نقطة مالية صريحة، مثل إنشاء الفاتورة أو ترحيلها أو إنشاء سند قبض/صرف.

## 3. خريطة الدورة المحاسبية الحالية

### 3.1 فاتورة البيع

يُنشأ رأس فاتورة في `invoices` بحالة `draft` أو `posted`. عند الترحيل، يبحث الكود عن حساب العميل أو الوكيل، ثم ينشئ حركة مالية من نوع `invoice`، ويُنشئ قيداً مديناً للعميل أو للصندوق/البنك بحسب طريقة التسليم، وقيداً دائناً للإيراد. بعد ذلك يُستدعى `validate_journal_balance()` قبل تحويل الحركة إلى `posted` [3].

### 3.2 فاتورة الشراء

يبحث الكود عن حساب المورد، ثم عن حساب تكلفة الخدمة، وينشئ حركة من نوع `purchase`. القيد المتوقع هو مدين للتكلفة ودائن للمورد. ويُتحقق من توازن القيد قبل الترحيل [3].

### 3.3 سندات القبض والصرف

يحتفظ `financial_transactions` بنوع العملية، والحساب الطرفي، وحساب الصندوق/البنك، والعملة، وحالة الترحيل، وبيانات العكس. أما `payment_allocations` فيربط السند بالفاتورة ويمنع تخصيص السند نفسه على الفاتورة نفسها عبر قيد فريد على زوج المعرفين [2].

### 3.4 العكس والإلغاء

يوجد في النموذج حقول `reversal_transaction_id` و`original_voucher_id` و`reversal_voucher_id` و`is_reversed`، كما يوضح سجل SQL وجود سندات قبض بحالة `reversed` وسندات عكسية مقابلة. هذا هو الاتجاه المحاسبي الصحيح: **عدم حذف القيد المرحل، بل إنشاء قيد عكسي قابل للتتبع** [2].

## 4. نقاط القوة المكتشفة

| المجال | النتيجة |
|---|---|
| الفصل بين المسودة والترحيل | موجود في الفواتير والحركات المالية، مع حقول `posted_at` و`posted_by` |
| توازن القيد | يوجد تحقق برمجي قبل الترحيل، وقوادح تمنع القيم السالبة في `journal_lines` |
| سجل التدقيق | `audit_logs` يحفظ المستخدم والعملية والجدول والسجل والقيم القديمة والجديدة وIP وUser-Agent |
| الفترات المالية | `fiscal_periods` تحفظ بداية ونهاية الفترة وحالة الإغلاق ومن أغلقها ومتى |
| تعدد العملات | الحسابات والأرصدة وبنود القيود تحمل `currency_id`، ويوجد سعر صرف ومبلغ أساسي |
| قابلية العكس | النموذج يحتوي حقول العكس، وسجل البيانات يثبت وجود عكس لسندات قبض |
| منع التكرار في الدفع | `payment_allocations` لديه قيد فريد على السند والفاتورة |
| حماية طبقة الأعمال | تقرير الإصلاح السابق يثبت إغلاق فحوصات الصلاحيات والفترة والتدقيق في البيئة المعزولة [1] |

## 5. الملاحظات المحاسبية الحرجة

### 5.1 اختلاف معادلة صافي الفاتورة بين القاعدة والكود

قوادح `invoices` تحسب `net_amount` على أساس: `total_amount - discount + tax_amount`. أما مسار ترحيل فاتورة البيع في `accounting_functions.php` فيستخدم `total_amount - discount` فقط. إذا استُخدمت الضرائب مستقبلاً، فقد يختلف صافي رأس الفاتورة عن مبلغ القيد المرحل. يجب توحيد المعادلة في خدمة واحدة واختبارها للضرائب والخصومات والاسترداد.

### 5.2 معالجة `amount_received` تحتاج مراجعة

في مسار البيع، التعبير البرمجي يحوّل قيمة `amount_received` إلى `float` قبل تطبيق البديل عند `null`. عملياً قد تتحول القيمة الفارغة إلى صفر، فلا يعمل الافتراض المقصود بالصورة المتوقعة. هذا قد يسبب قيداً ناقصاً عند فاتورة نقدية لا تحمل قيمة قبض صريحة. يجب اعتماد قاعدة واضحة: إما أن يكون المقبوض صفراً فعلاً، أو يُملأ بصافي الفاتورة قبل إنشاء القيد، مع اختبار الحالات النقدية والتحويل والآجل.

### 5.3 غياب المفاتيح الأجنبية المحاسبية الفعلية

توجد فهارس وعلاقات منطقية بين `financial_transactions` و`journal_lines` و`unified_accounts` و`payment_allocations`، لكن التقرير البنيوي السابق يثبت أن بعض هذه الروابط ليست مفاتيح أجنبية فعلية [2]. هذا يرفع خطر السجلات اليتيمة عند أي حذف مباشر أو ترحيل غير منضبط. لا يُنصح بإضافة مفاتيح أجنبية مباشرة إلى الإنتاج قبل فحص السجلات اليتيمة في نسخة احتياطية وكتابة Migration مع Rollback.

### 5.4 تكرار الفهارس

يظهر في المخطط تكرار أو تداخل لعدد من الفهارس على `branch_id`، ورقم الحركة، والحسابات والأرصدة [2]. لا يمثل ذلك خللاً محاسبياً فورياً، لكنه يزيد تكلفة الكتابة وقد يعقّد خطة الصيانة. المعالجة الآمنة هي قياس الاستخدام وخطط التنفيذ أولاً، ثم حذف الفهارس المكررة في Migration منفصل قابل للعكس.

### 5.5 حساسية أرصدة الحسابات المجمعة

يُحدّث `account_balances_unified` بتجميع بنود القيد بحسب الحساب والعملة، ثم ضرب الفرق في سعر الصرف لتكوين الرصيد الأساسي [3]. هذا تصميم قابل للعمل، لكنه حساس لأي إعادة ترحيل مزدوجة أو عكس غير متناسق أو اختلاف في `branch_id`. يجب أن تكون عملية الترحيل idempotent، وأن تمنع إعادة تطبيق الأرصدة على حركة `posted` سبق تطبيقها.

## 6. مؤشرات من نسخة SQL الموجودة في المستودع

النسخة الموجودة ليست بديلاً عن القراءة من قاعدة البيانات الفعلية، لكنها تقدم مؤشرات مهمة يجب تأكيدها بالمصالحة. من أمثلتها وجود فواتير مبيعات ومشتريات مرحّلة مرتبطة بخدمات العمرة والحج والجوازات والحجوزات، ووجود قيود يومية متوازنة ظاهرياً للأرقام المسجلة، ووجود سند قبض مرحل بقيمة 900 مرتبط بالفاتورة `SI-000001` عبر `payment_allocations` [2]. كما تُظهر العينة حالات مسودة وأخرى مرحّلة، ما يؤكد أن البيانات تحتوي على دورة حياة فعلية وليست مجرد جداول فارغة.

في المقابل، توجد في العينة سجلات اختبار واضحة مثل أرقام `INV-TEST-*` ووصف «اختبار توافق إجراء الفاتورة». يجب عزل هذه السجلات أو وسمها في بيئة التشغيل وفق سياسة معتمدة، وعدم حذفها من الإنتاج قبل تحديد أثرها على القيود والأرصدة والتدقيق.

## 7. خطة المصالحة المطلوبة قبل الإنتاج

ينبغي تنفيذ الفحوص التالية على نسخة قراءة فقط أو Staging مطابق، مع تصدير النتائج إلى ملف مؤرخ وموقع تجزئة. لا تُنفذ أي معالجة تصحيحية أثناء الفحص.

| الفحص | معيار النجاح |
|---|---|
| الفواتير المرحّلة بلا حركة مالية مقابلة | صفر سجلات |
| الحركات المالية المرحّلة بلا قيد يومي | صفر سجلات |
| كل حركة مالية متوازنة | مجموع المدين = مجموع الدائن بنفس العملة والفرع |
| قيود تحتوي مديناً ودائناً سالباً | صفر سجلات |
| تخصيصات دفعات تتجاوز صافي الفاتورة | صفر سجلات |
| تخصيصات على سند غير مرحّل أو فاتورة ملغاة | صفر سجلات |
| فواتير مدفوعة دون قبض/تخصيص كافٍ | صفر سجلات أو تفسير موثق |
| فواتير ذات `net_amount` مخالف للمعادلة | صفر سجلات أو اعتماد ضريبي موثق |
| حركات عكسية بلا أصل أو أصل بلا عكس مكتمل | صفر سجلات |
| أرصدة مجمعة لا تساوي مجموع القيود المرحّلة | فرق صفري بعد مراعاة العملة وسعر الصرف |
| سجلات تشغيلية تشير إلى فواتير غير موجودة | صفر سجلات |
| مستخدمون أو فروع أو عملات غير صالحة في مستندات مالية | صفر سجلات |

المصالحة يجب أن تكون على مستويين: مستوى تفصيلي لكل مستند وقيد، ومستوى إجمالي لكل حساب وعملة وفرع وفترة. أي فرق لا يُعالج بتعديل مباشر؛ بل يُوثق سببه، ويُنشأ له إجراء تصحيحي مستقل مع موافقة وRollback.

## 8. الأمن والتدقيق

الإصلاحات الأخيرة أغلقت الثغرات الحرجة التي كانت في صفحات النسخ الاحتياطي والإعدادات عبر صلاحيات صريحة وCSRF في طلبات POST، وأضيفت آلية تحقق من صلاحية الصفحة من قاعدة البيانات بحسب التقرير المرحلي. هذا جيد، لكن اعتماد الإنتاج يجب أن يتحقق من أن كل نقاط الدخول البديلة، وطلبات AJAX، وأي ملفات CLI أو تصدير، تمر عبر الحماية نفسها.

يجب التأكد عملياً من أن فشل `audit_logs` يمنع العملية المالية ولا يسمح بنجاح صامت، وأن السجل يتضمن هوية المستخدم والعملية والمستند والقيم قبل وبعد وIP وUser-Agent. كما يجب اختبار منع إعادة إرسال POST، ومنع الوصول المباشر إلى ملفات النسخ الاحتياطي، وعدم عرض رسائل SQL أو stack trace للمستخدم النهائي.

## 9. نتيجة اختبار الاتصال في هذه الجولة

تم تشغيل اختبار PHP المحلي الموجود في المشروع قراءة فقط. أظهر الاختبار رسالة `Connection refused`، رغم أن بيئة التشغيل أظهرت مستمعاً محلياً على منفذ MySQL. ويشير نص أداة الاختبار إلى سياق سابق يتعلق بالمنفذ 3307، ما يعني أن هناك احتمال اختلاف بين إعداد PHP CLI وإعداد خادم PHP أو ملف الإعدادات المستخدم فعلياً. لذلك لم يتم تنفيذ استعلامات الجداول على قاعدة البيانات الحية، ولم تُعتبر نسخة SQL الموجودة دليلاً كافياً على الحالة الحالية.

الإجراء الصحيح التالي هو تحديد مصدر إعداد الاتصال الفعلي من بيئة الخادم، ثم فتح اتصال قراءة فقط إلى قاعدة `alghazali` أو توفير Dump حديث/نسخة Staging. لا ينبغي تغيير المنفذ أو بيانات الاتصال عشوائياً في الإنتاج.

## 10. خطة Rollback وعدم تغيير البيانات

قبل أي Migration أو تنظيف بيانات، يجب أخذ نسخة احتياطية كاملة واختبار استعادتها في بيئة منفصلة. تُنفذ التغييرات المحاسبية في Migrations idempotent صغيرة، وتُسجل قبل/بعد، ويُمنع حذف القيود المرحّلة. عند فشل اختبار القبول، يُعاد نشر نسخة التطبيق السابقة، وتُحفظ السجلات، ولا تُجرى قيود تعويضية أو تعديلات أرصدة يدوية إلا من خلال خطة استرداد معتمدة.

## 11. الأولويات التنفيذية

الأولوية الأولى هي إصلاح تضارب معادلة `net_amount` ومعالجة `amount_received`، ثم تنفيذ مصالحة القراءة فقط على القاعدة الفعلية. الأولوية الثانية هي اختبار idempotency للترحيل والعكس والتخصيص في جميع الخدمات. الأولوية الثالثة هي فحص السجلات اليتيمة وتكرار الفهارس، ثم وضع Migrations منفصلة فقط بعد اعتماد النتائج. وبعد ذلك يُنفذ اختبار قبول وظيفي على سيناريوهات نقد، تحويل بنكي، آجل، خصم، إلغاء، واسترداد في بيئة Staging.

## الخلاصة

النظام يمتلك أساساً محاسبياً منظماً وسجل تدقيق ومسار ترحيل وعكس، والإصلاحات الأمنية السابقة قوية على مستوى الكود المعزول. لكن **الجاهزية الإنتاجية لم تثبت بعد** بسبب عدم الوصول إلى قاعدة البيانات الفعلية في هذه الجولة، وبسبب ملاحظتي الاتساق الخاصتين بالضريبة والمقبوض، إضافة إلى مخاطر العلاقات المنطقية دون Foreign Keys. التوصية المهنية هي إبقاء القرار **NO-GO للإنتاج غير المشروط** إلى أن تُنفذ مصالحة القراءة فقط وتُغلق الملاحظات المذكورة بتغييرات صغيرة قابلة للاختبار والرجوع.

## المراجع

[1]: ./FINANCE_PRODUCTION_READINESS_REPORT_V2_20260807.md "تقرير الجاهزية المالية وإغلاق إصلاحات P0"

[2]: ./DATABASE_SAFETY_REPORT.md "تقرير سلامة قاعدة البيانات"

[3]: ./includes/accounting_functions.php "المسار المحاسبي الفعلي للفواتير والأرصدة"

[4]: ./tools/database/alghazali.sql "مخطط ونسخة SQL الموجودة في المستودع"
