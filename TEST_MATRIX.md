# مصفوفة اختبار منصة رِحلة

**قاعدة التسجيل:** لا تُسجل PASS إلا بعد تنفيذ الاختبار فعليًا. الحالات التي تحتاج قاعدة بيانات أو جلسة متصفح أو مزودًا خارجيًا تُسجل NOT RUN مع السبب.

| المجال | العنصر | الاختبار | النتيجة الحالية | الحالة |
|---|---|---|---|---|
| PHP | جميع ملفات PHP | فحص syntax آلي | لا توجد أخطاء | PASS |
| JavaScript | `assets/js/app.js` | `node --check` | لا توجد أخطاء syntax | PASS |
| الحماية | `.htaccess` | مطابقة النسخة المحلية والبعيدة | مطابقة كاملة | PASS |
| الحماية | `uploads` | منع تنفيذ PHP/PHTML/PHAR | قواعد المنع موجودة | PASS |
| الحماية | الملفات الداخلية | منع الوصول إلى config/includes/classes/database | قواعد المنع موجودة | PASS |
| OTP | توليد رمز 4/6/8 أرقام | اختبار PHP مستقل | ناجح | PASS |
| OTP | رقم يمني محلي | أرقام عربية وصفر محلي | ناجح | PASS |
| OTP | رقم دولي بصيغة 00 | تطبيع إلى صيغة + | ناجح | PASS |
| OTP | حد إعادة الإرسال | مراجعة وإصلاح مجموع `send_count` | تم الإصلاح وفحص الكود | PASS |
| SEO | `robots.txt` | طلب HTTPS مباشر | HTTP 200 | PASS |
| SEO | `sitemap.xml` | طلب HTTPS مباشر | مهلة متقطعة | FAIL/HOST |
| الصفحة الرئيسية | `index.php` | فتح حي | مهلة اتصال | NOT RUN/HOST |
| تسجيل الدخول | `login.php` | فتح حي وتسجيل دخول | مهلة اتصال | NOT RUN/HOST |
| لوحة الإدارة | `admin/admin.php` | فتح حي | مهلة اتصال | NOT RUN/HOST |
| API | `route=health` | طلب GET حي | مهلة اتصال | NOT RUN/HOST |
| المدن | إضافة مدينة | قاعدة بيانات وجلسة مدير | يحتاج بيئة تكامل | NOT RUN |
| الدول | إضافة وتعديل | قاعدة بيانات وجلسة مدير | يحتاج بيئة تكامل | NOT RUN |
| المحطات | إضافة وتعديل الموقع | قاعدة بيانات وجلسة مدير | يحتاج بيئة تكامل | NOT RUN |
| المسارات | إنشاء مسار رئيسي وفرعي | قاعدة بيانات وجلسة مدير | يحتاج بيئة تكامل | NOT RUN |
| الرحلات | إنشاء رحلة ومقاعد | قاعدة بيانات وجلسة مدير | يحتاج بيئة تكامل | NOT RUN |
| الحجوزات | حجز مقعد | قاعدة بيانات واختبار E2E | يحتاج بيئة تكامل | NOT RUN |
| الدفع | الدفع اليدوي/البوابة | مزود دفع وبيئة اختبار | لم يُنفذ على الإنتاج | NOT RUN |
| التذاكر | إصدار وطباعة | حجز مكتمل وجلسة | يحتاج بيئة تكامل | NOT RUN |
| الصلاحيات | Agent/Admin/Customer | جلسات وأدوار حقيقية | يحتاج حسابات اختبار | NOT RUN |
| التقارير | إجماليات وفلاتر | مقارنة مباشرة بقاعدة البيانات | يحتاج DB اختبار | NOT RUN |
| الرفع | صور صحيحة/خاطئة | اختبار متصفح وملفات | يحتاج جلسة متصفح | NOT RUN |
| التجاوب | هاتف/لوحي/كمبيوتر | مراجعة بصرية كاملة | يحتاج تشغيل صفحات PHP | NOT RUN |

## بوابة الإصدار

لا يُعتمد الإصدار التجاري النهائي قبل تحويل حالات `NOT RUN/HOST` إلى نتائج فعلية بعد استقرار PHP/MySQL وتوفير قاعدة اختبار معزولة.
