
## نتائج اختبار 2026-08-26

- نافذة حجوزات الرحلة TR-000002 أعادت حجزين عبر API، وظهر زر «عرض التفاصيل» لكل صف.
- تفاصيل BK-2026-860382 عادت بحالة HTTP 200 وتضمنت العميل والمسافر والمقعد والهاتف والجواز وتاريخ الميلاد وموعدي الرحلة والدفع والمصدر. تم إصلاح خطأ SQL سببه طلب عمود غير موجود في booking_segments.
- زر إغلاق تفاصيل الحجز أخفى النافذة دون تغيير حالة الحجز.
- لوحة التحكم أعادت HTTP 200 بعد إصلاح معاملات PDO المتكررة، وظهرت مقارنات «هذا الشهر/الشهر السابق» وبطاقتا الوكلاء والشركات، مع أعمدة تقييم العملاء في التصنيفات.
- زر إشعارات الإدارة يفتح نافذة منبثقة فعلية، وعند فتحها تحولت الإشعارات غير المقروءة إلى مقروءة وظهر العداد 0.
- تمت تهيئة إشعاري new_booking محليًا لاختبار العداد التلقائي؛ لم يتم تعديل الاستضافة.

أثبت اختبار محلي بإشعار جديد أن دورة polling التقطت المعرف الجديد وفتحت نافذة الإشعارات تلقائيًا، مع بقاء غير المقروء حتى يفتح المستخدم الزر. تم حذف إشعار الاختبار وإرجاع حالات الإشعارات القديمة إلى قيمها الأصلية. كما حُصرت أزرار تأكيد/رفض الحجز على دور الإدارة فقط.

## الخلاصة النهائية

تمت إعادة اختبار لوحة التحكم بعد الإصلاحات. زر الإشعارات أصبح يفتح نافذة منبثقة فعلية، ويُظهر عدد غير المقروء، وعند الفتح اليدوي تُعلّم العناصر كمقروءة. كما تم اختبار ظهور النافذة تلقائيًا بإشعار محلي جديد ثم حذف سجل الاختبار وإرجاع حالات الإشعارات الأصلية.

تم اختبار تفاصيل BK-2026-860382 بعد إصلاح الاستعلام، وظهرت بيانات العميل والمسافر والوثائق والمقعد والدفع والمواعيد. وتم اختبار BK-2026-001171 المعلق، فظهرت أزرار «تأكيد الحجز وإصدار التذاكر» و«رفض الحجز» داخل نافذة التفاصيل. زر الإغلاق أغلق الشاشة فقط ولم يلغِ الحجز.

أعادت dashboard/overview استجابة HTTP 200، وظهرت بطاقات هذا الشهر والشهر السابق وبطاقتا الوكلاء والشركات، مع تقييم العملاء وعدد المراجعات في التصنيفات. نجح فحص PHP لكل ملفات includes وAPI، ونجح فحص JavaScript للملفين app.js وpublic-template.js. أخطاء سجل الخادم المشار إليها قبل الإصلاحات كانت تاريخية؛ لا توجد أخطاء 500 جديدة في اختبارات ما بعد الإصلاح.

## تحديثات متابعة تجربة العميل ولوحة التحكم — 2026-08-26

- أضيف حفظ حالة الحجز في `sessionStorage` واستعادتها بعد تحديث الصفحة لشاشات المقاعد وبيانات المسافر والتذكرة. تم اختبار الاستعادة فعليًا على الرحلة `TR-000002`، وعادت الصفحة إلى مخطط المقاعد مع 40 مقعدًا و38 متاحًا.
- أضيف جرس العميل في الهيدر مع عداد غير المقروء ونافذة «إشعاراتي»، ومسارا «حجوزاتي» و«إشعاراتي» للحساب العميل. تم تقييدها بحساب العميل الحقيقي (`customer_id` أو دور `customer`) حتى لا يرى المدير إشعارات العميل في الصفحة العامة.
- تم إصلاح CSRF بعد تسجيل الخروج؛ يجلب التطبيق رمزًا جديدًا من `health`، ثم نجح إنشاء عميل محلي اختباري وظهر الجرس والنافذة، وبعد الاختبار نُظف الحساب بالكامل.
- تم تصحيح ربط السعر العام بالمقطع الفرعي للرحلة. اختبار API يعيد الآن `TR-000002` مرة واحدة بسعر `60000.00`، وتعرض البطاقة العامة `٦٠٬٠٠٠ ر.ي`.
- ضُبطت لوحة التحكم افتراضيًا على «هذا الأسبوع» مع مقارنة «الأسبوع السابق»، وتبقى «هذا الشهر» و«الشهر السابق» خيارات مستقلة من الفلتر. أضيفت تنسيقات بطاقات متجاوبة مستوحاة من AdminLTE RTL.
- اختُبر إشعار إداري محلي يتضمن اسم العميل والحالة داخل النافذة المنبثقة والعداد، ثم حُذف إشعار الاختبار دون المساس بالإشعارات الأصلية.
- فحوص `php -l` و`node --check` نجحت بعد التعديلات.
