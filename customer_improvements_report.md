# تقرير تحسين تجربة العميل — مشروع رِحلة

## الحالة

تم تنفيذ التعديلات على النسخة المحلية فقط في `/home/ubuntu/rihla_app`. لم يتم الرفع إلى الاستضافة أو GitHub، ولم تُحذف بيانات حقيقية.

## ما تم تنفيذه

| المجال | النتيجة |
|---|---|
| الرحلات المنتهية | أصبحت الرحلات العامة والبحث يعتمدان على وقت الخادم، ولا تظهر الرحلة بعد بدء موعد مغادرتها. |
| بطاقات الرحلات | تعرض الشركة والشعار، المدينة، التاريخ واسم اليوم، وقت الحضور، وقت المغادرة، المقاعد المتاحة، السعر الحالي، ونوع الرحلة؛ أزيلت عبارة طلب الحجز من البطاقة. |
| الشركات المتاحة | العدد والأسماء تُحسب من الرحلات المطابقة فعليًا لنتيجة البحث. |
| حجز الزائر | الضغط على «احجز الآن» يفتح الدخول/التسجيل مع حفظ الرحلة والمقطع والمقاعد، ثم يعيد فتح شاشة المقاعد بعد نجاح الدخول. |
| حجوزاتي | مفعلة من القائمة الجانبية وتستخدم API الحالي للحجوزات والتذاكر. |
| إشعارات العميل | أضيفت القائمة والعداد والتحديث الدوري كل 30 ثانية، مع تعليم الإشعار كمقروء. هذا polling وليس Push/WebSocket. |
| إشعار حالة الرحلة | يرسل للعميل عند التغير الفعلي للحالة، ومنها الإلغاء والإكمال، دون تكرار عند حفظ الحالة نفسها. |
| التقييمات | متاحة بعد حالة `completed` ومرور موعد المغادرة، مع منع التكرار وإظهار متوسط التقييم وعدده في بيانات الرحلة والشركة. |
| الأسعار | أضيف `trip_price_change_logs` لحفظ السعر السابق والحالي ونوع التغير، مع إعداد `show_price_change_badge` لإظهار شارة الزيادة أو التخفيض. |
| قاعدة البيانات القديمة | يضمن التطبيق وجود `passengers.gender` وجدول `trip_reviews` عند تشغيله، مع وجود ملفات migrations مستقلة. |

## ملفات مهمة

- `assets/js/app.js`
- `assets/js/public-template.js`
- `assets/css/app.css`
- `assets/css/public-template.css`
- `includes/ReferenceService.php`
- `includes/BookingService.php`
- `includes/AdminService.php`
- `includes/TripDisplaySettingsService.php`
- `api/v1/index.php`
- `database/trip_price_change_logs_migration.sql`
- `database/trip_display_settings_migration.sql`
- `database/customer_reviews_migration.sql`
- `database/passenger_gender_migration.sql`
- `customer_improvements_report.md`

## الاختبارات

نجحت فحوص PHP وJavaScript، واختبارات `health` و`site-settings` و`trip-display-public` و`countries` و`companies` و`routes` و`contact-channels` و`trips/upcoming` و`trips/search`. أعاد endpoint الإشعارات للزائر HTTP 401 كما يجب. بعد تجاوز وقت رحلات 2026-08-26 ظهرت في الصفحة العامة رحلات 2026-08-27 فقط. كما ظهر اختبار زر الحجز نافذة الدخول برسالة حفظ المقاعد.

اختبار سجل السعر كان قابلًا للتراجع: تغيرت قيمة 20,000 إلى 20,001 مؤقتًا، سُجلت كزيادة، ثم أعيدت القيمة الأصلية ونُظفت سجلات الاختبار.

## رابط الاختبار

[فتح النسخة المحلية في Chrome](https://8082-ituwvcui1itwqkvzmub3u-ebbdc61d.us3.manus.computer/)

الرابط مؤقت وقد يتوقف عند انتهاء بيئة الاختبار.
