(function () {
  'use strict';
  const carouselState = new Map();
  const localDate = (value) => new Date(String(value || '').replace(' ', 'T'));
  const safeDate = (value, options, fallback = '—') => { const date = localDate(value); return Number.isNaN(date.getTime()) ? fallback : new Intl.DateTimeFormat('ar-YE', options).format(date); };
  const clock = (value) => safeDate(value, { hour: 'numeric', minute: '2-digit' });
  const routeAttendance = (value) => {
    const match = String(value || '').match(/^(\d{2}):(\d{2})/);
    if (!match) return '';
    const hour = Number(match[1]); const period = hour < 12 ? 'صباحًا' : 'مساءً'; const displayHour = hour % 12 || 12;
    return `${String(displayHour).padStart(2, '0')}:${match[2]} ${period}`;
  };
  const initials = (value) => String(value || 'ر').trim().slice(0, 1);
  const unique = (items) => [...new Set(items.filter(Boolean))];
  function header(ctx, active = 'home') {
    const accountLabel = ctx.state.user ? 'حسابي' : 'القائمة';
    return `<header class="site-header"><div class="nav-container"><a class="brand" href="/" data-public-page="home"><span class="brand-icon"><i class="bi bi-bus-front-fill"></i></span><span class="brand-text">منصة <span>رحلة</span></span></a><nav class="nav-links" id="public-nav-links"><button class="nav-link ${active === 'home' ? 'active' : ''}" data-public-page="home">الرئيسية</button><button class="nav-link ${active === 'about' ? 'active' : ''}" data-public-page="about">من نحن</button><button class="nav-link ${active === 'contact' ? 'active' : ''}" data-public-page="contact">اتصل بنا</button><button class="nav-link ${active === 'privacy' ? 'active' : ''}" data-public-page="privacy">السياسة والخصوصية</button></nav><div style="display:flex;align-items:center;gap:7px"><button class="nav-account account-menu-trigger" type="button" data-action="open-account-drawer" aria-label="فتح ${ctx.esc(accountLabel)}"><i class="bi bi-person-circle"></i></button><button class="nav-toggle" id="public-nav-toggle" type="button" aria-label="فتح القائمة" aria-expanded="false"><i class="bi bi-list"></i></button></div></div></header>`;
  }
  function footer() { return `<footer class="site-footer"><div class="footer-grid"><div><div class="footer-brand"><span class="brand-icon"><i class="bi bi-bus-front-fill"></i></span><strong>منصة رحلة</strong></div><p class="footer-desc">منصة عربية لحجز رحلات الباصات؛ ابحث عن الرحلة المتاحة، واختر مقعدك، ثم أرسل طلب الحجز من خلال التدفق الآمن في النظام.</p></div><div class="footer-col"><h4>روابط سريعة</h4><button data-public-page="home">الرئيسية</button><button data-public-page="about">من نحن</button><button data-public-page="contact">اتصل بنا</button></div><div class="footer-col"><h4>معلومات الاستخدام</h4><button data-public-page="privacy">السياسة والخصوصية</button><button data-public-page="home">البحث عن رحلة</button></div></div><div class="footer-bottom">© ${new Date().getFullYear()} منصة رحلة</div></footer>`; }
  function cityOptions(ctx, selected = '') { return `<option value="">اختر المدينة</option>${ctx.state.cities.map((city) => `<option value="${ctx.esc(city.id)}" ${String(city.id) === String(selected) ? 'selected' : ''}>${ctx.esc(city.name_ar)}</option>`).join('')}`; }
  function busTypes(ctx) { const fromResults = (ctx.state.search?.trips || []).map((trip) => trip.bus_type); const fromCompanies = (ctx.state.companies || []).flatMap((company) => String(company.bus_types || '').split('|')); return unique([...fromResults, ...fromCompanies].map((type) => String(type || '').trim())).sort((a, b) => a.localeCompare(b, 'ar')); }
  function busTypeOptions(ctx, selected = '') { const types = busTypes(ctx); return `<option value="">كل الأنواع</option>${types.map((type) => `<option value="${ctx.esc(type)}" ${String(type) === String(selected) ? 'selected' : ''}>${ctx.esc(type)}</option>`).join('')}`; }
  function searchForm(ctx, values = {}) { const ready = ctx.state.cities.length > 0; const typeReady = busTypes(ctx).length > 0; const date = values.date || new Date(Date.now() + 86400000).toISOString().slice(0, 10); return `<div class="search-wrapper"><form class="search-card" id="public-search-form" novalidate><div class="search-grid"><div class="search-field field-from"><label for="public-from"><i class="bi bi-geo-alt-fill"></i> من</label><select class="search-input" id="public-from" name="origin_city_id" required ${ready ? '' : 'disabled'}>${cityOptions(ctx, values.origin_city_id)}</select></div><div class="field-swap"><button class="swap-btn" type="button" id="public-swap" aria-label="تبديل مدينتي الانطلاق والوصول"><i class="bi bi-arrow-left-right"></i></button></div><div class="search-field field-to"><label for="public-to"><i class="bi bi-geo-alt"></i> إلى</label><select class="search-input" id="public-to" name="destination_city_id" required ${ready ? '' : 'disabled'}>${cityOptions(ctx, values.destination_city_id)}</select></div><div class="search-field field-date"><label for="public-date"><i class="bi bi-calendar-event"></i> تاريخ الرحلة</label><input class="search-input" id="public-date" name="date" type="date" min="${new Date().toISOString().slice(0, 10)}" value="${ctx.esc(date)}" required></div><div class="search-field field-type"><label for="public-bus-type"><i class="bi bi-bus-front"></i> نوع الباص</label><select class="search-input" id="public-bus-type" name="bus_type" ${typeReady ? '' : 'disabled'}>${busTypeOptions(ctx, values.bus_type)}</select></div><div class="field-btn"><button class="search-btn" type="submit" ${ready ? '' : 'disabled'}><i class="bi bi-search"></i> بحث</button></div></div></form></div>`; }
  function bindChrome(ctx) {
    const toggle = ctx.$('#public-nav-toggle'); const links = ctx.$('#public-nav-links');
    toggle?.addEventListener('click', () => { const open = links.classList.toggle('open'); toggle.setAttribute('aria-expanded', String(open)); });
  }
  function bindSearch(ctx) {
    const form = ctx.$('#public-search-form');
    ctx.$('#public-swap')?.addEventListener('click', () => { const from = ctx.$('#public-from'); const to = ctx.$('#public-to'); const previous = from.value; from.value = to.value; to.value = previous; });
    form?.addEventListener('submit', async (event) => { event.preventDefault(); const query = Object.fromEntries(new FormData(form)); if (!query.origin_city_id || !query.destination_city_id) return ctx.showToast('اختر مدينة الانطلاق والوصول أولًا.', 'error'); if (query.origin_city_id === query.destination_city_id) return ctx.showToast('لا يمكن أن تكون مدينة الانطلاق هي مدينة الوصول.', 'error'); await ctx.searchTrips(query); });
  }
  function emptyState(message, button = '') { return `<div class="public-empty"><i class="bi bi-binoculars"></i><h2>${message}</h2><p>استخدم نموذج البحث لاختيار مدينة الانطلاق والوجهة وتاريخ السفر.</p>${button}</div>`; }
  function companyFallback(company) { return `<span aria-label="شعار بديل لـ ${company}">${initials(company)}</span>`; }
  function tripImages(ctx, trip) { return unique([ctx.mediaPath(trip.exterior_image_path), ctx.mediaPath(trip.interior_image_path)]); }
  function seatStatus(trip) { const available = Math.max(0, Number(trip.available_seats ?? trip.total_seats ?? 0)); const total = Math.max(available, Number(trip.total_seats || 0)); const ratio = total ? available / total : 0; const level = available === 0 ? 'full' : ratio <= .1 ? 'low' : ratio <= .3 ? 'medium' : 'high'; return { available, total, ratio, level, text: available === 0 ? 'مكتملة' : `${available} مقعدًا متاحًا من ${total}` }; }
  function carousel(ctx, trip) { const images = tripImages(ctx, trip); const index = carouselState.get(Number(trip.id)) || 0; if (!images.length) return `<div class="card-media"><div class="media-fallback" aria-label="لا تتوفر صورة باص لهذه الرحلة"><i class="bi bi-bus-front"></i></div></div>`; const slides = images.map((image, position) => `<img class="carousel-slide" src="${image}" alt="صورة باص رحلة ${ctx.esc(trip.trip_number)} رقم ${position + 1}" loading="lazy">`).join(''); const controls = images.length > 1 ? `<button class="carousel-btn carousel-btn-prev" type="button" data-carousel="prev" data-trip-id="${ctx.esc(trip.id)}" aria-label="الصورة السابقة"><i class="bi bi-chevron-right"></i></button><button class="carousel-btn carousel-btn-next" type="button" data-carousel="next" data-trip-id="${ctx.esc(trip.id)}" aria-label="الصورة التالية"><i class="bi bi-chevron-left"></i></button><div class="carousel-dots">${images.map((_, position) => `<button class="carousel-dot ${position === index ? 'active' : ''}" type="button" data-carousel-index="${position}" data-trip-id="${ctx.esc(trip.id)}" aria-label="عرض الصورة ${position + 1} من ${images.length}" aria-current="${position === index ? 'true' : 'false'}"></button>`).join('')}</div>` : ''; return `<div class="card-media" id="public-carousel-${ctx.esc(trip.id)}"><div class="carousel-track" style="transform:translateX(-${index * 100}%);">${slides}</div>${controls}</div>`; }
  function renderTripCard(ctx, trip) { const seat = seatStatus(trip); const logo = ctx.mediaPath(trip.logo_path); const attendance = routeAttendance(trip.attendance_time); const busMeta = [trip.bus_type, trip.bus_name, trip.model_year ? String(trip.model_year) : ''].filter(Boolean).join(' · '); const fullBadge = seat.available === 0 ? '<span class="approved-badge full">مكتملة</span>' : ''; const cardButton = seat.available === 0 ? '<button class="approved-book" type="button" disabled>مكتملة</button>' : `<button class="approved-book" type="button" data-trip="${ctx.esc(trip.id)}" data-segment="${ctx.esc(trip.segment_id)}">احجز الآن <i class="bi bi-arrow-left"></i></button>`; return `<article class="approved-trip-card"><div style="position:relative">${carousel(ctx, trip)}<div class="card-badges">${fullBadge}</div></div><div class="approved-card-body"><div class="approved-company-row"><div class="approved-company-logo">${logo ? `<img src="${logo}" alt="شعار ${ctx.esc(trip.company_name)}">` : companyFallback(ctx.esc(trip.company_name))}</div><div class="approved-company-info"><div class="approved-company-name" title="${ctx.esc(trip.company_name)}">${ctx.esc(trip.company_name)}</div><div class="approved-company-meta">${trip.bus_type ? `<span class="bus-type-tag">${ctx.esc(trip.bus_type)}</span>` : ''}<span>${ctx.esc(trip.trip_number)}</span></div></div></div><div class="approved-trip-date"><i class="bi bi-calendar-event"></i><span>${ctx.esc(safeDate(trip.departure_at, { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' }))}</span></div><div class="approved-route"><div class="approved-point departure"><div class="approved-city">${ctx.esc(trip.origin_name)}</div><div class="approved-time-group">${attendance ? `<div><span class="approved-time-label">وقت الحضور</span><span class="approved-time-value">${ctx.esc(attendance)}</span></div>` : ''}<div><span class="approved-time-label">وقت المغادرة</span><span class="approved-time-value departure">${ctx.esc(clock(trip.departure_at))}</span></div></div></div><div class="approved-route-connector" aria-label="من ${ctx.esc(trip.origin_name)} إلى ${ctx.esc(trip.destination_name)}"><div class="approved-route-line"><span class="approved-route-bus" aria-hidden="true"><i class="bi bi-bus-front-fill"></i></span></div></div><div class="approved-point arrival"><div class="approved-city">${ctx.esc(trip.destination_name)}</div></div></div><div class="approved-seat-section"><div class="approved-seat-info"><span class="approved-seat-label"><i class="bi bi-grid-3x3-gap"></i> المقاعد المتاحة</span><span class="approved-seat-count ${seat.level}">${ctx.esc(seat.text)}</span></div><div class="approved-progress ${seat.level}" role="progressbar" aria-label="المقاعد المتاحة" aria-valuemin="0" aria-valuemax="${seat.total}" aria-valuenow="${seat.available}"><span style="width:${Math.round(seat.ratio * 100)}%"></span></div></div><div class="approved-card-footer"><div><span class="approved-price-label">السعر للفرد</span><strong class="approved-price">${ctx.money(trip.amount, trip.currency_symbol)}</strong>${busMeta ? `<button class="approved-view" type="button" data-view-trip="${ctx.esc(trip.id)}">تفاصيل الباص والرحلة</button>` : ''}</div>${cardButton}</div></div></article>`; }
  function displayedTrips(ctx) { const selected = String(ctx.state.search?.bus_type || ''); const list = ctx.state.search?.trips || []; return selected ? list.filter((trip) => String(trip.bus_type || '') === selected) : list; }
  function bindResults(ctx) { ctx.$$('[data-trip]').forEach((button) => button.addEventListener('click', () => ctx.chooseTrip(Number(button.dataset.trip), Number(button.dataset.segment)))); ctx.$$('[data-view-trip]').forEach((button) => button.addEventListener('click', () => ctx.viewTrip(Number(button.dataset.viewTrip)))); ctx.$$('[data-carousel]').forEach((button) => button.addEventListener('click', () => { const id = Number(button.dataset.tripId); const trip = (ctx.state.search?.trips || []).find((item) => Number(item.id) === id); const images = trip ? tripImages(ctx, trip) : []; if (images.length < 2) return; const current = carouselState.get(id) || 0; carouselState.set(id, (current + (button.dataset.carousel === 'next' ? 1 : -1) + images.length) % images.length); renderResults(ctx); })); ctx.$$('[data-carousel-index]').forEach((button) => button.addEventListener('click', () => { carouselState.set(Number(button.dataset.tripId), Number(button.dataset.carouselIndex)); renderResults(ctx); })); }
  async function renderHome(ctx) { ctx.root.innerHTML = `<div class="rihla-public">${header(ctx)}<main><section class="hero"><div class="hero-inner"><span class="hero-badge"><i class="bi bi-shield-check"></i> منصة حجز رحلات الباصات</span><h1 class="hero-title">احجز رحلتك بسهولة وأمان<br>بين المدن المتاحة</h1><p class="hero-subtitle">ابحث عن الرحلة المناسبة، واختر مقعدك، ثم أرسل طلب الحجز عبر خطوات واضحة.</p></div></section>${searchForm(ctx)}<section class="features-strip"><div class="feature-item"><span class="feature-icon blue"><i class="bi bi-search"></i></span><div class="feature-text"><strong>بحث واضح</strong><span>اختر المسار والتاريخ</span></div></div><div class="feature-item"><span class="feature-icon green"><i class="bi bi-grid-3x3-gap"></i></span><div class="feature-text"><strong>اختيار المقعد</strong><span>اعرض المقاعد المتاحة</span></div></div><div class="feature-item"><span class="feature-icon orange"><i class="bi bi-ticket-perforated"></i></span><div class="feature-text"><strong>طلب الحجز</strong><span>استكمل بيانات المسافرين</span></div></div><div class="feature-item"><span class="feature-icon red"><i class="bi bi-building"></i></span><div class="feature-text"><strong>شركات متاحة</strong><span>تُعرض من بيانات النظام</span></div></div></section><section class="results-section"><div class="results-head"><h2 class="results-title">الرحلات المتاحة</h2><span class="results-count">ابدأ بالبحث</span></div>${emptyState('اختر مسارك وتاريخ السفر')}</section></main>${footer()}</div>`; bindChrome(ctx); try { await ctx.preloadPublic(); const form = ctx.$('#public-search-form'); if (form) { form.outerHTML = searchForm(ctx); bindSearch(ctx); } } catch (error) { ctx.showToast(error.message, 'error'); } }
  function renderResults(ctx) { const trips = displayedTrips(ctx); const query = ctx.state.search || {}; ctx.root.innerHTML = `<div class="rihla-public">${header(ctx)}<main><section class="hero"><div class="hero-inner"><span class="hero-badge"><i class="bi bi-search"></i> نتائج مرتبطة ببيانات النظام</span><h1 class="hero-title">الرحلات المتاحة</h1><p class="hero-subtitle">راجع وقت الرحلة والمقاعد المتاحة والسعر ثم تابع لاختيار مقعدك.</p></div></section>${searchForm(ctx, query)}<section class="results-section"><div class="results-head"><h2 class="results-title">الرحلات المتاحة</h2><span class="results-count">${trips.length === 1 ? 'رحلة واحدة' : `${trips.length} رحلات`}</span></div>${trips.length ? `<div class="trip-grid">${trips.map((trip) => renderTripCard(ctx, trip)).join('')}</div>` : emptyState('لا توجد رحلات مطابقة')}</section></main>${footer()}</div>`; bindChrome(ctx); bindSearch(ctx); bindResults(ctx); }
  function renderLoading(ctx) { ctx.root.innerHTML = `<div class="rihla-public">${header(ctx)}<main class="results-section">${emptyState('جارٍ البحث في الرحلات المتاحة')}</main>${footer()}</div>`; bindChrome(ctx); }
  function renderError(ctx, query, message) { ctx.root.innerHTML = `<div class="rihla-public">${header(ctx)}<main class="results-section">${emptyState(ctx.esc(message || 'تعذر إتمام البحث الآن'), `<button class="search-btn" id="retry-public-search" type="button">إعادة المحاولة</button>`)}</main>${footer()}</div>`; bindChrome(ctx); ctx.$('#retry-public-search')?.addEventListener('click', () => ctx.searchTrips(query)); }
  function renderPage(ctx, page) { let content = ''; if (page === 'about') content = `<section class="inner-page"><div class="inner-head"><h1>من نحن</h1><p>منصة رقمية تساعد المسافرين على البحث عن رحلات الباصات المتاحة في النظام واستكمال طلب الحجز.</p><div class="head-line"></div></div><div class="public-info-grid"><div class="public-info-card"><i class="bi bi-search"></i><strong>ابحث</strong><span>حدد المسار وتاريخ السفر</span></div><div class="public-info-card"><i class="bi bi-grid-3x3-gap"></i><strong>اختر مقعدك</strong><span>راجع المقاعد المتاحة</span></div><div class="public-info-card"><i class="bi bi-ticket-perforated"></i><strong>أرسل طلبك</strong><span>أكمل بيانات المسافرين</span></div></div><div class="public-copy"><h2>كيف تعمل المنصة؟</h2><p>ابدأ بتحديد مدينة الانطلاق ومدينة الوصول وتاريخ السفر. عند ظهور رحلة مناسبة، انتقل إلى مخطط المقاعد ثم سجّل الدخول لإرسال طلب الحجز وفق قواعد النظام الحالية.</p></div></section>`; else if (page === 'contact') content = `<section class="inner-page"><div class="inner-head"><h1>اتصل بنا</h1><p>لا تنشر المنصة قنوات اتصال أو نموذج مراسلة ما لم تكن مرتبطة بخدمة فعلية ومعتمدة.</p><div class="head-line"></div></div><div class="public-copy"><h2>قنوات التواصل</h2><p>لم تُعدّ قناة تواصل عامة للتحقق والنشر بعد. لا تعرض هذه الصفحة بيانات اختبار أو أرقامًا أو بريدًا تجريبيًا من بيانات النظام.</p></div></section>`; else content = `<section class="inner-page"><div class="inner-head"><h1>السياسة والخصوصية</h1><p>تُستخدم بيانات الحساب والحجز لتقديم خدمات المنصة وإدارة طلبات الحجز.</p><div class="head-line"></div></div><div class="public-copy"><h2>بيانات الحساب والحجز</h2><p>يتطلب إتمام طلب الحجز تسجيل الدخول وإدخال بيانات المسافرين المطلوبة للتذكرة. لا تعرض هذه الصفحة وعودًا أو قنوات اتصال غير منشورة في النظام.</p></div></section>`; ctx.root.innerHTML = `<div class="rihla-public">${header(ctx, page)}<main>${content}</main>${footer()}</div>`; bindChrome(ctx); }
  window.RihlaPublicTemplate = { isActive: true, header, renderHome, renderResults, renderLoading, renderError, renderPage };
})();
