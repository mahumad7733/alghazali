# تقرير تسليم — تحسين البحث والحجوزات والدفع والبنوك

## نطاق التنفيذ
تم تعديل النسخة المحلية الحالية من مشروع «رِحلة» دون إنشاء مشروع جديد ودون رفع الملفات إلى الاستضافة أو GitHub. شملت التعديلات الصفحة العامة، تجربة حجوزات العميل، تفاصيل الحجوزات، إعدادات الرحلات، وإضافة إدارة الحسابات البنكية.

| المجال | ما تم تنفيذه |
|---|---|
| الصفحة العامة | حذف بطاقات «بحث واضح»، «مقاعد متاحة»، «تذاكر واضحة»، و«شركات متاحة» أسفل البحث، مع إبقاء ملخص الشركات المحسوب من الرحلات الظاهرة فقط. |
| التاريخ | إضافة زرين أسفل نموذج البحث لليوم والغد، ويظهر مع كل زر اسم اليوم والتاريخ العربي الكامل. |
| الوضع الليلي والنهاري | تحسين زر التبديل ليعرض الأيقونة والنص المناسبين: «ليلي» أو «نهاري»، مع تحديث النص المساعد والوضع المحفوظ في المتصفح. |
| حجوزاتي | أصبح زر «عرض» للحجز المؤكد أو المكتمل يفتح التذكرة مباشرة إذا كانت التذاكر قد صدرت. الحجوزات غير المؤكدة تفتح تفاصيلها وحالتها. |
| التذكرة | تعرض حالة الطلب، رقم الحجز، الرحلة، المقاعد، الإجمالي، طريقة الدفع، والحساب البنكي عند استخدام التحويل. بعد تأكيد الإدارة تظهر رسالة إصدار التذكرة بدل رسالة الطلب فقط. |
| إدارة الحجز | استبدال نافذة prompt بحوار منظم لزر «إلغاء الحجز»، يتضمن حقل سبب إلزاميًا، ويُرسل السبب للعميل في الإشعار وتفاصيل الحجز. أصبح الزر المقابل «تأكيد المسافر وإصدار التذاكر». |
| طرق الدفع | إضافة الدفع عند الوكيل، الدفع في الشركة، والتحويل البنكي. عند التحويل يجب اختيار حساب بنكي نشط وبعملة مطابقة لعملة الرحلة. |
| إعدادات الدفع | إضافة مفاتيح تشغيل وإيقاف طرق الدفع الثلاثة داخل إعدادات الرحلة، مع رابط مباشر لإدارة البنوك. |
| البنوك | إضافة صفحة `/admin/banks.php` لإضافة الحساب البنكي وتعديله وتفعيله أو إيقافه، مع البنك واسم صاحب الحساب ورقم الحساب وIBAN والفرع والعملة والملاحظات. |
| قاعدة البيانات | إضافة `database/payment_banks_migration.sql`، وتحديث `database/schema.sql`، مع تهيئة تلقائية للحقول الجديدة في البيئة المحلية. |

## مسارات الاستخدام

| الاستخدام | الرابط المحلي المؤقت |
|---|---|
| الموقع العام | [فتح الموقع](https://8082-ituwvcui1itwqkvzmub3u-ebbdc61d.us3.manus.computer/customer.php) |
| إعدادات طرق الدفع | [إعدادات الرحلة](https://8082-ituwvcui1itwqkvzmub3u-ebbdc61d.us3.manus.computer/admin/settings.php?section=trips) |
| إدارة البنوك والحسابات | [صفحة البنوك](https://8082-ituwvcui1itwqkvzmub3u-ebbdc61d.us3.manus.computer/admin/banks.php) |
| إدارة الحجوزات | [الحجوزات](https://8082-ituwvcui1itwqkvzmub3u-ebbdc61d.us3.manus.computer/admin/bookings.php) |

## نتائج الاختبار
أُعيد تشغيل فحوص الصياغة، ونجحت فحوص `node --check` لملفي `app.js` و`public-template.js`، كما نجحت فحوص `php -l` للملفات المعدلة في خدمات الحجوزات والبنوك وإعدادات الرحلات وواجهة API وملفات الصفحات الجديدة.

أعاد API الخاص بـ `health` و`payment-options` و`trips/upcoming` نتيجة نجاح. أعاد `payment-options` إعدادات طرق الدفع الثلاثة مفعلة وقائمة بنوك فارغة، لأن حسابًا بنكيًا حقيقيًا لم يُضف إلى قاعدة الاختبار. ظهرت الرحلة `TR-000002` مرة واحدة بسعر `60000.00` في اختبار الرحلات.

تم التحقق بصريًا من الصفحة العامة؛ بطاقات المزايا القديمة غير ظاهرة، وشريط اليوم والغد يعرض اسم اليوم والتاريخ، وزر الوضع بدّل الواجهة فعليًا من «ليلي» إلى «نهاري» عند النقر. كما فُتحت صفحة البنوك وإعدادات الرحلة بصريًا بحساب المدير، وظهر رابط «البنوك» في القائمة الجانبية ومفاتيح الدفع الثلاثة.

لم يتم إنشاء حجز أو حساب بنكي تجريبي أثناء هذا الاختبار حتى لا تترك بيانات اختبار في قاعدة البيانات. لذلك تم اختبار تدفق واجهة الدفع وAPI والتهيئة، بينما يلزم إضافة حساب بنكي فعلي من صفحة البنوك ثم اختبار إنشاء حجز فعلي إذا أردت اختبار التحويل البنكي بنهاية كاملة.

## الملفات الأساسية المعدلة
تم تعديل `assets/js/app.js` و`assets/js/public-template.js` و`assets/css/app.css` و`assets/css/public-template.css` و`includes/BookingService.php` و`includes/BankService.php` و`includes/TripDisplaySettingsService.php` و`api/v1/index.php` و`admin/_layout.php` و`customer.php`، مع إضافة `admin/banks.php` و`database/payment_banks_migration.sql`.

## ملاحظة النشر
هذه التعديلات محلية فقط. لم يتم رفعها إلى InfinityFree أو GitHub، ولم يتم تضمين أي كلمات مرور أو مفاتيح أو إعدادات اتصال حساسة في التقرير.


أضاف الاختبار الختامي تأكيدًا إضافيًا: ظهر رابط «البنوك» فعليًا في القائمة الجانبية، ونجح جلب تفاصيل حجز مؤكد من جلسة المدير مع قائمة تذاكر تحتوي تذكرة واحدة دون خطأ SQL. لم تتغير بيانات الحجز أثناء الاختبار.

## تحديث 2026-08-27 — بيانات العميل والتقييمات والإدارة

تمت إضافة صفحة «بياناتي» للعميل مع حفظ آخر بيانات المسافر وإعادة تعبئتها تلقائيًا في الحجز التالي، مع إمكان تعديل الاسم والهاتف والجنس والجواز وتاريخ ومكان الميلاد وصورة الحساب. أضيف نموذج تقييم بعد اكتمال الرحلة لتقييم الرحلة والشركة والوكيل عند وجود وكيل، مع تعليق واحد محفوظ ويظهر للمخولين في لوحة التحكم.

تمت إضافة زر «أرى العملاء» في سياق الشركة لعرض العملاء والتقييمات والتعليقات المسموح بها. كما أضيفت نافذة «حجز إداري» لاختيار الرحلة والمقطع والمقاعد وبيانات المسافرين وطريقة الدفع، وصُحح عرض المقاطع ليستخدم محطات الانطلاق والوصول التي يعيدها API فعليًا.

في نموذج الوكيل أزيل اختيار الشركة من الواجهة، وتصبح الشركة تلقائية من نطاق حساب المدير أو أول شركة نشطة للمدير الرئيسي. أصبح رمز الوكيل اختياريًا، وإذا أُدخل فيجب أن يكون عشرة أرقام، وأضيف تأكيد كلمة المرور مرتين. وفي نموذج العملة أضيف مربع اختيار مستقل لتحديد العملة الافتراضية بدل إجبار المستخدم على حقل العملة الأساسية. أضيفت ملفات الترحيل `database/agent_code_migration.sql` و`database/currency_default_migration.sql`، مع تحديث `schema.sql`.

نجحت الفحوص النهائية لـ `node --check` و`php -l`، ونجحت API health وtrips/upcoming وتفاصيل الرحلة، بينما يعيد الملف الشخصي غير الموثق HTTP 401 كما هو مطلوب. بصريًا ظهر شريط التاريخ بصيغة الرقم فوق اسم اليوم: «اليوم ٢٧ الخميس» و«غدًا ٢٨ الجمعة»، واختفت بطاقات المزايا القديمة، وظهر نموذج الوكيل دون اختيار الشركة، ونموذج العملة مع مربع العملة الافتراضية، ونافذة الحجز الإداري بخيارات الرحلة والمقطع والمقاعد والدفع. لم يُحفظ وكيل أو عملة أو حجز تجريبي أثناء الاختبار.
