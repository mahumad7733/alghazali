# تقرير إصلاح الحجوزات وتدقيق الدفع وOTP

**المشروع:** منصة رِحلة — PHP/MariaDB/JavaScript/Bootstrap RTL  
**تاريخ المراجعة:** 28 أغسطس 2026  
**النطاق:** النسخة المحلية فقط؛ لم يتم رفع هذه الجولة إلى الاستضافة.

## 1. سبب تعطل صفحة الحجوزات

الرابط `https://rihla.kesug.com/bookings.php` كان يعيد صفحة 404 من InfinityFree لأن الملف العام `bookings.php` لم يكن موجودًا في المشروع. منطق حجوزات العميل نفسه كان موجودًا داخل `assets/js/app.js` في الدالة `showCustomerBookings()` ويستخدم مسار API `bookings` بعد التحقق من جلسة العميل.

أضيف مدخل عام صغير باسم `bookings.php` يمرر إلى `customer.php?page=bookings`. كما أضيفت صفحة `bookings` إلى القائمة المسموحة في `customer.php` وإلى renderer العام. النتيجة المحلية المثبتة هي HTTP 200، وفتح الصفحة في Chrome يعرض نافذة تسجيل الدخول للزائر بدل 404. بعد تسجيل الدخول تُستدعى قائمة الحجوزات الحالية من API، ولا يُنشأ حجز جديد.

## 2. نتيجة فحص الدفع الحالية

الحالة المحلية الحالية آمنة لكنها غير مفعلة للدفع الإلكتروني: يوجد سجل Moyasar في بيئة Sandbox لكنه معطل، ولا يوجد secret key أو webhook secret محفوظ. طرق الدفع اليدوية الحالية تعمل، و`payment-options` أعاد نجاحًا، وكانت القنوات اليدوية الثلاث مفعلة، مع وجود حساب بنكي يدوي بعملة YER. لم أقم بإضافة أي مفتاح أو بيانات تاجر تجريبية.

| البند | الحالة المحلية | الحكم |
|---|---|---|
| SAR | موجودة في migration والأساس المالي | جاهزة برمجيًا، لا تُجعل افتراضية تلقائيًا |
| Moyasar Sandbox | موجودة ومعطلة | صحيح وآمن قبل إدخال بيانات التاجر |
| Moyasar Live | لا توجد إعدادات أو مفاتيح | غير مفعلة، وهذا هو الوضع الصحيح حاليًا |
| Hosted Invoice | المحول موجود ويحسب المبلغ من الحجز | يحتاج مفاتيح Sandbox واختبار تاجر حقيقي |
| Webhook | endpoint وتحقق secret وidempotency محلي موجودان | أضيفت مطابقة `live` للبيئة، ولم يُختبر خارجيًا |
| Refund | خدمة كامل/جزئي وحالات pending/completed/failed موجودة | لا يمكن اختبار رد الأموال الحقيقي بلا payment ID ومفتاح تاجر |
| VAT/ZATCA | معطلة افتراضيًا | لا توجد نسبة مفترضة ولا تكامل ZATCA مفعّل |

## 3. توافق Moyasar ووسائل الدفع السعودية

من ناحية التصميم، يستخدم المحول Basic Auth عبر HTTPS، ويحفظ secret key في الخادم، ولا يرسل بيانات البطاقة إلى Backend المنصة. يستخدم Hosted Invoice بمبلغ integer بوحدة العملة الصغرى ورمز ISO-4217 وحقول انتهاء وعودة. هذه العناصر متوافقة مع التوثيق الرسمي المتاح لميسر.

التوافق مع مدى وVisa وMastercard وApple Pay وSTC Pay وSamsung Pay لا يعني أن الوسيلة مفعلة تلقائيًا في حساب المنصة. التفعيل يعتمد على حساب التاجر، العقد، إعدادات ميسر، النطاق، الجهاز، وتهيئة الوسيلة لدى التاجر. لذلك فإن حقل `methods` في لوحة الإدارة توثيقي، وليس دليلًا على أن الوسيلة مقبولة فعليًا.

أُصلح خطأ في إدخال دفعة Hosted Checkout الجديدة كان يمنع حفظ `gateway_environment` بصورة صحيحة. كما أزيل رأس idempotency داخلي غير موثق للفواتير المستضافة لدى ميسر، وأضيفت metadata مرجعية للحجز ومحاولة الدفع. الحماية المحلية تمنع النقر المتكرر، ولا تُدّعي idempotency خاصة بميسر للفواتير المستضافة دون توثيق صريح. توثيق ميسر يثبت `given_id` لإنشاء الدفع، بينما صفحة الفاتورة المستضافة لا تثبت استخدام هذا الحقل بالطريقة نفسها.

## 4. نتيجة فحص OTP

OTP غير مفعّل محليًا. WhatsApp وSMS غير مهيئين، والبريد الإلكتروني كان مفعّلًا في القيمة القديمة لكن دون from_email أو SMTP، ولذلك عُدّل الحساب العام للقنوات بحيث لا يعرض للمستخدم أي قناة غير مكتملة. أصبحت لوحة الإدارة تميز بين السر المحفوظ وبين القناة الجاهزة فعليًا.

أُضيف استخدام SecretVault AES-256-GCM للأسرار الجديدة، مع قراءة متوافقة للقيم القديمة المشفرة بنمط CBC حتى لا تنكسر الإعدادات الموجودة. لا يظهر الرمز أو السر أو التجزئة في واجهة الإدارة أو سجل OTP. لم يُرسل أي رمز حقيقي أثناء الاختبار.

## 5. توحيد الإعدادات

أصبحت بطاقات إعدادات الدفع وOTP والبنوك ضمن نافذة «تهيئة النظام» في لوحة الإدارة. أُخفي تكرارها من القائمة الجانبية الرئيسية، مع بقاء الصفحات المباشرة متاحة للتوافق. عند فتح إعدادات الدفع أو OTP أو البنوك يبقى زر «تهيئة النظام» محددًا، ويمكن فتح كل قسم من بطاقة واحدة بحسب صلاحيات المستخدم.

## 6. الاختبارات المنفذة

نجحت فحوص PHP syntax للملفات المعدلة: `bookings.php` و`customer.php` و`PaymentService.php` و`MoyasarGateway.php` و`OtpService.php`. نجحت فحوص JavaScript syntax لـ`app.js` و`public-template.js`.

أعاد الرابط المحلي `/bookings.php` HTTP 200 وظهر عنوان «حجوزاتي». فتح الرابط الخارجي المؤقت في Chrome أظهر نافذة تسجيل الدخول بدل 404. أعاد `health` نجاحًا، وأعاد `payment-options` نجاحًا، وأعاد مسار قنوات OTP إعدادًا معطلًا وقنوات فارغة. اختبارات الوحدة والـwebhook واختبار migration نجحت، وبقيت أعداد baseline دون تغيير: 11 حجزًا، 8 مدفوعات، و0 استردادات.

لم تُختبر عملية دفع حقيقية مع Moyasar بسبب عدم وجود مفاتيح Sandbox، ولم تُختبر رسائل WhatsApp أو SMS أو البريد بسبب عدم وجود مزود وبيانات إرسال حقيقية. لذلك لا يصح اعتبار الدفع الإلكتروني أو OTP جاهزًا للإنتاج بعد.

## 7. الملفات المحلية المعدلة

| الملف | الغرض |
|---|---|
| `bookings.php` | مدخل عام متوافق لصفحة حجوزات العميل |
| `customer.php` | السماح بصفحة `bookings` العامة |
| `assets/js/public-template.js` | فتح حجوزات العميل من renderer العام |
| `assets/js/app.js` | تصحيح payment row، بيئة webhook، طرق الدفع العامة، وجمع الإعدادات |
| `includes/PaymentService.php` | تصحيح بيئة الدفع وmetadata والخيارات العامة |
| `includes/MoyasarGateway.php` | مواءمة Hosted Invoice مع التوثيق الرسمي المتاح |
| `includes/OtpService.php` | جاهزية القنوات وتشفير الأسرار الجديد |
| `current_browser_findings.md` | سجل الاختبارات المؤكدة |

## 8. ما يلزم قبل الرفع والتفعيل

يجب أولًا إعداد مفتاح تشفير خادم قوي في `config.php` المحلي والاستضافة، ثم إدخال مفاتيح Moyasar Sandbox من Dashboard عبر صفحة إعدادات الدفع، وإدخال webhook secret من إعدادات Webhook لدى ميسر، وضبط عنوان `app_url` العام. بعد ذلك يجب تسجيل webhook بعنوان API الموجود، واختبار paid/failed/refunded من Sandbox، ثم اختبار عودة المستخدم مع بقاء webhook هو مصدر الحقيقة.

لتفعيل OTP يجب اختيار مزود حقيقي، وتحديد API URL وtoken أو إعداد SMTP وfrom_email، ثم اختبار قناة واحدة بعنوان يملكه المشغل. لا تُفعّل القناة العامة قبل نجاح الاختبار. لا توجد مفاتيح في هذا التقرير ولا في ملفات الواجهة.

## 9. حدود قانونية وتجارية

وجود محول API لا يثبت ترخيصًا أو اعتمادًا سعوديًا. يلزم onboarding وKYC وحساب تاجر وعقد مزود دفع، كما يلزم التحقق من متطلبات النطاق وApple Pay ووسائل الدفع لدى ميسر. كما أن إعداد VAT والفوترة ليس استشارة ضريبية أو قانونية، ويجب مراجعة محاسب أو مستشار سعودي قبل تفعيل نسبة أو إصدار فواتير ضريبية أو بدء أي مسار ZATCA.

## المراجع الرسمية

[1]: https://moyasar.com/ar/ "موقع ميسر الرسمي العربي"
[2]: https://docs.moyasar.com/api/authentication "Moyasar Authentication"
[3]: https://docs.moyasar.com/api/invoices/01-create-invoice "Moyasar Create Invoice"
[4]: https://docs.moyasar.com/api/other/webhooks/webhook-reference "Moyasar Webhook Reference"
[5]: https://docs.moyasar.com/api/idempotency "Moyasar Idempotency"
[6]: https://docs.moyasar.com/api/payments/05-refund-payment "Moyasar Refund Payment"
[7]: https://docs.moyasar.com/api/payments/payment-status-reference "Moyasar Payment Status Reference"
