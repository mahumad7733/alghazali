# التقنية وطريقة العمل في منصة حجوزات الباصات

## التقنية المستخدمة

| الطبقة | التقنية | الاستخدام الفعلي |
|---|---|---|
| الواجهة العامة والإدارة | **PHP 8** وHTML وCSS | صفحات الموقع وبوابة الدخول وصفحات الإدارة المنفصلة تحت `/admin/`. |
| الواجهة التفاعلية | **JavaScript Vanilla** | تحميل البيانات من API، النوافذ المنبثقة، النماذج، التصفية، الاختيارات المتسلسلة، والجداول RTL. |
| واجهة التكامل | **REST API** في `api/v1/index.php` | مسارات JSON موحدة للمصادقة والإدارة والرحلات والحجوزات والوكلاء. |
| منطق الأعمال | خدمات PHP مثل `AdminService` و`BookingService` و`AgentService` | تحقق الأعمال، العزل بين الشركات، إنشاء الرحلات، الحجز، العمولة، الرصيد، وسجل التدقيق. |
| قاعدة البيانات | **MySQL/MariaDB** عبر PDO | جداول المستخدمين والأدوار والصلاحيات والشركات والمدن والمسارات والباصات والرحلات والحجوزات والمحافظ والعمولات. |
| الحماية | جلسات PHP، CSRF، `password_hash`، RBAC | دخول بالبريد أو اسم المستخدم أو الجوال، مع أدوار وصلاحيات وحماية خادمية لكل صفحة إدارية. |
| الاستضافة | **InfinityFree** | النسخة الحية على `https://rihla.kesug.com`، والرفع يتم إلى مجلد النطاق الصحيح عبر FTP. |

> النظام ليس قالبًا جاهزًا منفصلًا عن البيانات؛ الواجهة والخدمات وREST وقاعدة البيانات متصلة ببعضها داخل نفس مشروع PHP/MySQL.

## طريقة التنفيذ المتبعة

نعمل بأسلوب تدريجي يحمي البيانات الحالية. يبدأ كل تطوير بفحص المخطط والخدمات والواجهات الموجودة، ثم نضيف أقل تغيير ممكن إلى قاعدة البيانات مع ملف ترقية واضح. بعد ذلك يُضاف التحقق في الخادم أولًا، ثم تُبنى الواجهة العربية RTL فوق المسار نفسه بدل إنشاء نظام موازٍ.

| خطوة العمل | ما يتم تطبيقه |
|---|---|
| فحص قبل التعديل | قراءة الجداول والعلاقات والخدمات الفعلية، والتحقق من بيانات قاعدة الاختبار المحلية. |
| ترقية آمنة | إضافة أعمدة أو فهارس فقط عند الحاجة، دون حذف سجلات أو تغيير علاقات قائمة. |
| حماية الخادم | التحقق من الدور والصلاحية ونطاق الشركة وCSRF داخل API والخدمة، وليس في الواجهة فقط. |
| معاملات قاعدة البيانات | العمليات المركبة، مثل إنشاء التكرار، تُنفذ داخل Transaction؛ إما تنجح كلها أو تتراجع كلها عند الخطأ. |
| سجل التدقيق | العمليات الإدارية الحساسة تسجل في `audit_logs` لتسهيل المراجعة لاحقًا. |
| اختبار محلي | فحص PHP وJavaScript والاختبارات البنيوية ثم اختبار الواجهة في المتصفح قبل الرفع. |
| اختبار حي ورفع | بعد اكتمال الحماية والاختبارات، تُرفع الملفات المحددة إلى InfinityFree ثم يتم التحقق من النسخة الحية. |

## ما أُنجز في المهمة الحالية

تم تحسين صفحة **المسارات الفرعية**. أصبحت تستخدم دقائق كاملة في الوقت، وتقترح وقت المغادرة بعد 30 دقيقة من وقت الحضور، وتدعم عبور منتصف الليل مثل 11:45 مساءً إلى 12:15 صباحًا. كما أصبح تعديل المسار الفرعي المرتبط بمسار رئيسي مسموحًا من دون حذف الرابط. أضيف سعر بيع وسعر داخلي للشركة وهامش ربح؛ السعر الداخلي لا يظهر ولا يُعدل إلا للصلاحيات المالية.

تم تطوير نموذج **الرحلات** ليعمل بالتسلسل: شركة، ثم مسار رئيسي، ثم مسار فرعي مرتبط فقط، ثم باص تابع للشركة. عند اختيار تاريخ الرحلة يعبئ النظام موعدي المغادرة والوصول من المقطع المختار، ويمنع الخادم اختيار مقطع لا يتبع المسار الرئيسي.

تم تنفيذ إنشاء **الرحلات المتكررة** كسجلات فعلية بين تاريخين وفق أيام الأسبوع، مع معاينة قبل الإنشاء وتأكيد صريح. في الاختبار المحلي أُنشئت ثلاث رحلات اختبارية بالرموز `TESTREC-001` إلى `TESTREC-003`، وكل رحلة نُسخ لها 20 مقعدًا وثلاثة أسعار مقاطع، وجُمعت تحت معرف تكرار واحد. هذه سجلات محلية للاختبار وليست مرفوعة إلى الاستضافة بعد.

## المهام الحالية المتبقية

| المجال | الحالة الحالية | العمل المتبقي |
|---|---|---|
| التعديل الجماعي للرحلات | تم بناء الواجهة والخادم ومعاينة قبل التنفيذ | إكمال اختبار التعديل الجماعي للحالة والموعد والباص، مع التحقق من حظر الرحلات التي لديها حجوزات. |
| لقطات الأسعار والمالية | الأعمدة والترقية الأساسية موجودة | ربط سعر الشركة وسعر البيع والعمولة وعمولة التطبيق بحفظ الحجز، كي لا تتغير الحجوزات القديمة عند تعديل الأسعار المستقبلية. |
| الوكلاء والحسابات | المحفظة والحد الائتماني والعمولات موجودة أساسًا | تعزيز عروض الأرصدة وكشف الحساب والعمولات والقيود المالية حسب الصلاحيات. |
| العروض والتقارير | صفحات قائمة موجودة | إضافة الفلاتر والتجميعات المطلوبة للرحلات والحسابات والتقارير. |
| الاختبارات | فحوص الصياغة والبنية اجتازت | توسيع اختبارات التكرار والتعديل الجماعي واللقطات المالية والصلاحيات. |
| النشر الحي | لم يبدأ لهذا الإصدار | رفع الترقية والملفات بعد اكتمال الاختبارات، ثم التحقق من `rihla.kesug.com` وحفظ نقطة استعادة. |

## مبدأ حماية البيانات

لا يُحذف أي سجل قائم تلقائيًا، ولا تُعاد بناء المسارات الرئيسية أو الفرعية بلا داعٍ. تعديلات الأسعار الجديدة تُهيأ لتؤثر في الاستخدام المستقبلي فقط؛ أما الحجز المؤكد فيجب أن يحتفظ بلقطة أسعاره وعمولاته عند إنشاء الحجز. كما لا يسمح التعديل الجماعي بتغيير موعد أو باص رحلة لديها حجز، بينما يمكن إدارة حالتها وفق الصلاحيات.
